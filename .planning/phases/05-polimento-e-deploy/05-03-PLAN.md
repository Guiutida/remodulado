---
phase: 05-polimento-e-deploy
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - assets/js/ui.js
  - assets/css/base.css
  - pages/aluno.html
  - pages/professor.html
  - pages/atividades.html
  - pages/trilhas.html
  - pages/turma.html
  - pages/configuracoes.html
  - pages/estudo.html
  - assets/js/aluno.js
  - assets/js/atividades.js
  - assets/js/professor.js
  - assets/js/painel.js
  - assets/js/turma.js
  - assets/js/trilhas.js
  - assets/js/trilhas-ia.js
autonomous: true
requirements: [INFRA-05]

must_haves:
  truths:
    - "Spinner global aparece em tela durante fetch e some no finally{} — nunca fica preso"
    - "Erro de rede exibe toast vermelho com mensagem amigável — nenhuma tela em branco silenciosa"
    - "Sucesso exibe toast verde com mensagem de confirmação"
    - "Botão de submit fica desabilitado e muda texto durante fetch; restaura no finally{}"
    - "Listas que falham ao carregar mostram .erro-inline em vez de ficarem vazias"
    - "ui.js não redefine mostrarAviso() — zero conflito com aluno.js"
  artifacts:
    - path: "assets/js/ui.js"
      provides: "showLoading(), hideLoading(), showError(msg), showSuccess(msg)"
      contains: "showLoading"
    - path: "assets/css/base.css"
      provides: "CSS do spinner (.carregando-global) + toast (.aviso-config.sucesso/.erro) + erro inline"
      contains: "carregando-global"
  key_links:
    - from: "pages/*.html (7 páginas)"
      to: "assets/js/ui.js"
      via: "script src ../assets/js/ui.js ANTES de aluno.js"
      pattern: "ui.js"
    - from: "assets/js/*.js (fetch wrappers)"
      to: "window.showLoading / window.hideLoading"
      via: "try { showLoading() } finally { hideLoading() }"
      pattern: "showLoading"
    - from: "assets/js/*.js (catch blocks)"
      to: "window.showError"
      via: "catch(erro) { showError(mensagem amigável) }"
      pattern: "showError"
---

<objective>
Criar utilitário global ui.js com spinner, toasts e estado de botão; aplicar em todas as páginas e scripts de produto (exceto login/cadastro/ia-estudo).

Purpose: Nenhuma tela deve ficar em branco quando a API falha. O usuário deve receber feedback visual imediato de que algo está acontecendo ou deu errado.
Output: assets/js/ui.js, CSS adicionado em base.css, script tag em 7 páginas HTML, catch blocks atualizados em 7 arquivos JS.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-polimento-e-deploy/05-RESEARCH.md
@.planning/phases/05-polimento-e-deploy/05-UI-SPEC.md

<interfaces>
<!-- Contratos e restrições que o executor precisa conhecer -->

Globals de aluno.js (NÃO redeclarar em ui.js):
```js
window.mostrarAviso = function(texto) { ... }  // aluno.js linha 268 — NÃO tocar
window.usuarioLogado = function() { ... }
window.tokenAtual = function() { ... }
```

ui.js DEVE expor apenas estes novos globals:
```js
window.showLoading   = function() { ... }
window.hideLoading   = function() { ... }
window.showError     = function(msg) { ... }
window.showSuccess   = function(msg) { ... }
```

CSS existente que NÃO deve ser alterado (base.css linhas 762–773):
```css
.aviso-config {
  position: fixed; right: 24px; bottom: 24px; z-index: 30;
  border-radius: 12px; padding: 12px 16px;
  background: var(--texto); color: var(--branco);
  font-weight: 900; box-shadow: 0 14px 30px rgba(15, 35, 68, .22);
}
```

CSS a ADICIONAR em base.css (após .aviso-config):
```css
/* Toast variants (Phase 5) */
.aviso-config.sucesso { background: #166534; color: #f0fdf4; }
.aviso-config.erro    { background: #991b1b; color: #fef2f2; }

/* Erro inline em listas */
.erro-inline { color: #991b1b; font-weight: 700; font-size: 0.88rem; }
body.tema-escuro .erro-inline { color: #fca5a5; }

/* Spinner global */
.carregando-global {
  position: fixed; inset: 0; z-index: 50;
  display: none; flex-direction: column;
  align-items: center; justify-content: center; gap: 16px;
  background: rgba(15, 35, 68, 0.35);
}
.carregando-global.ativo { display: flex; }
.spinner-anel {
  width: 40px; height: 40px;
  border: 4px solid rgba(49, 87, 246, 0.2);
  border-top-color: var(--azul);
  border-radius: 50%;
  animation: girar 0.75s linear infinite;
}
@keyframes girar { to { transform: rotate(360deg); } }
.carregando-global span {
  color: #fff; font-weight: 700;
  font-size: 0.88rem; letter-spacing: 0.01em;
}
```

Páginas e seus scripts de produto (para referência ao inserir script tag):
- pages/aluno.html      → aluno.js + painel.js
- pages/professor.html  → aluno.js + professor.js
- pages/atividades.html → aluno.js + atividades.js
- pages/trilhas.html    → aluno.js + trilhas.js + trilhas-ia.js
- pages/turma.html      → aluno.js + turma.js
- pages/configuracoes.html → aluno.js
- pages/estudo.html     → aluno.js
- EXCLUÍDAS: login.html, cadastro.html (acesso.js já tem loading), ia-estudo.html (SSE usa indicador próprio)

Padrão de botão em estado de carregamento (formalizar em todos os formulários):
```js
const textoOriginal = botao.textContent;
botao.disabled = true;
botao.textContent = 'Salvando\u2026'; // '…' Unicode U+2026
try {
  showLoading();
  // ... fetch ...
} finally {
  hideLoading();
  botao.disabled = false;
  botao.textContent = textoOriginal;
}
```

Padrão para listas que falham ao carregar:
```js
lista.innerHTML = "<p class='erro-inline'>Não foi possível carregar. Tente novamente.</p>";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar assets/js/ui.js + adicionar CSS em base.css + inserir script tag nas 7 páginas HTML</name>
  <files>assets/js/ui.js, assets/css/base.css, pages/aluno.html, pages/professor.html, pages/atividades.html, pages/trilhas.html, pages/turma.html, pages/configuracoes.html, pages/estudo.html</files>
  <action>
**1. Criar assets/js/ui.js** — módulo IIFE, sem imports/exports, só assigns em window:

```js
// assets/js/ui.js
// Globals: showLoading(), hideLoading(), showError(msg), showSuccess(msg)
// Depende de: nada — deve ser carregado ANTES de aluno.js nas páginas
// NÃO redeclara mostrarAviso — aluno.js já a define globalmente

(function () {
  'use strict';

  // ─── Spinner global ──────────────────────────────────────────────────────
  function obterSpinner() {
    var el = document.querySelector('.carregando-global');
    if (!el) {
      el = document.createElement('div');
      el.className = 'carregando-global';
      el.setAttribute('role', 'status');
      el.setAttribute('aria-label', 'Carregando');
      el.setAttribute('aria-live', 'polite');
      el.innerHTML = '<div class="spinner-anel"></div><span>Carregando\u2026</span>';
      document.body.appendChild(el);
    }
    return el;
  }

  window.showLoading = function () {
    obterSpinner().classList.add('ativo');
  };

  window.hideLoading = function () {
    var el = document.querySelector('.carregando-global');
    if (el) el.classList.remove('ativo');
  };

  // ─── Toast ────────────────────────────────────────────────────────────────
  var toastTimer = null;

  function mostrarToast(msg, classe) {
    var antigo = document.querySelector('.aviso-config');
    if (antigo) antigo.remove();
    if (toastTimer) clearTimeout(toastTimer);

    var aviso = document.createElement('div');
    aviso.className = 'aviso-config' + (classe ? ' ' + classe : '');
    aviso.textContent = msg;
    document.body.appendChild(aviso);
    toastTimer = setTimeout(function () { aviso.remove(); }, 3000);
  }

  window.showError   = function (msg) { mostrarToast(msg, 'erro'); };
  window.showSuccess = function (msg) { mostrarToast(msg, 'sucesso'); };
  // showError/showSuccess são nomes novos — NÃO conflitam com mostrarAviso de aluno.js
})();
```

**2. Adicionar CSS em assets/css/base.css** — inserir IMEDIATAMENTE após o bloco `.aviso-config { ... }` (após a chave de fechamento da linha ~773), ANTES de `.modal-foto`:

Adicionar exatamente o CSS do bloco `<interfaces>` acima: variantes de toast (.aviso-config.sucesso/.aviso-config.erro), .erro-inline com dark mode, e o bloco completo do spinner (.carregando-global, .carregando-global.ativo, .spinner-anel, @keyframes girar, .carregando-global span).

**Atenção:** Não duplicar `@keyframes girar` se já existir em base.css. Checar antes com grep.

**3. Adicionar `<script src="../assets/js/ui.js"></script>` nas 7 páginas HTML:**

Em cada página, inserir a linha IMEDIATAMENTE ANTES da tag `<script src="../assets/js/aluno.js">`. Usar a tag `<script src="../assets/js/auth-guard.js">` como ponto de referência — a ordem deve ser: auth-guard.js → ui.js → aluno.js → (scripts de página).

Exemplo para pages/aluno.html (onde auth-guard não existe na busca atual — confirmar):
```html
<!-- ANTES de: -->
<script src="../assets/js/aluno.js"></script>
<!-- INSERIR: -->
<script src="../assets/js/ui.js"></script>
<script src="../assets/js/aluno.js"></script>
```

Fazer o mesmo nas outras 6 páginas: professor.html, atividades.html, trilhas.html, turma.html, configuracoes.html, estudo.html.
  </action>
  <verify>
    <automated>Test-Path "assets/js/ui.js"</automated>
    <automated>node -e "const s = require('fs').readFileSync('assets/js/ui.js','utf8'); if(!s.includes('showLoading')) throw new Error('showLoading missing'); if(s.includes('mostrarAviso')) throw new Error('mostrarAviso conflito!'); console.log('ui.js ok')"</automated>
    <automated>Select-String -Path "assets/css/base.css" -Pattern "carregando-global" -Quiet</automated>
    <automated>Select-String -Path "assets/css/base.css" -Pattern "aviso-config\.sucesso" -Quiet</automated>
    <automated>(Select-String -Path "pages/aluno.html","pages/professor.html","pages/atividades.html","pages/trilhas.html","pages/turma.html","pages/configuracoes.html","pages/estudo.html" -Pattern "ui\.js").Count</automated>
  </verify>
  <done>
  - assets/js/ui.js existe com showLoading, hideLoading, showError, showSuccess em IIFE
  - ui.js NÃO contém declaração de mostrarAviso
  - base.css contém .carregando-global e .aviso-config.sucesso
  - Todas as 7 páginas HTML contêm script tag para ui.js (verificar count = 7)
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Atualizar blocos catch e estados de carregamento nos 7 arquivos JS de produto</name>
  <files>assets/js/aluno.js, assets/js/atividades.js, assets/js/professor.js, assets/js/painel.js, assets/js/turma.js, assets/js/trilhas.js, assets/js/trilhas-ia.js</files>
  <action>
Aplicar as seguintes mudanças em cada arquivo. O objetivo é eliminar telas em branco silenciosas e substituir `<p><em>Carregando...</em></p>` por spinner + .erro-inline em falha.

**Regras gerais (aplicar em todos os arquivos):**
- Em funções assíncronas que fazem fetch: envolver com `try { showLoading(); ... } finally { hideLoading(); }` — EXCETO em `ia-estudo.js` que usa SSE próprio
- Nos blocos `catch`: substituir `mostrarAviso("Erro ao carregar X: " + erro.message)` por `showError("Não foi possível carregar. Tente novamente.")` — nunca expor `erro.message` técnico ao usuário
- Strings `<p><em>Carregando...</em></p>` e `<p>Carregando...</p>` → substituir por chamada `showLoading()` antes do fetch (lista fica vazia durante o loading, o spinner global comunica o estado)
- `<p class='erro-inline'>Não foi possível carregar. Tente novamente.</p>` para containers que precisam mostrar estado de erro inline (listas, grades)
- Para botões de submit em formulários: usar padrão `textoOriginal / botao.disabled = true / botao.textContent = 'Salvando…' / finally restaurar` — ja implementado em acesso.js; extender aos demais
- Funções que chamam `mostrarAviso(msg, "sucesso")` (turma.js, trilhas.js) → substituir por `showSuccess(msg)`
- Funções que chamam `mostrarAviso(msg, "erro")` (turma.js, trilhas.js linha 20) → substituir por `showError(msg)`
- `mostrarAviso(msg)` sem argumento de tipo → manter como está (informativo neutro, toast neutro); só tocar nos `catch` de erros de rede/API

**Por arquivo:**

**aluno.js:**
- `buscarPreferenciasBanco()` e `buscarUsuarioBanco()`: adicionar showLoading()/hideLoading() e trocar `mostrarAviso("Nao foi possivel carregar preferencias")` no catch por `showError("Não foi possível carregar preferências.")`
- `mostrarAviso("Não foi possível atualizar dados da conta")` → `showError("Não foi possível atualizar os dados da conta.")`
- Manter todos os outros `mostrarAviso()` chamados para confirmações de sucesso (ex: "Configuração salva", "Status atualizado") — eles são informativos, não erros

**atividades.js:**
- `lista.innerHTML = "<p><em>Carregando...</em></p>"` (linha 242) → remover (showLoading cobrirá o estado)
- `areaQ.innerHTML = "<p><em>Carregando questões...</em></p>"` (linha 429) → remover
- `feedback.innerHTML = "<p><em>Carregando respostas...</em></p>"` (linha 558) → remover
- Catch blocks com `mostrarAviso("Erro ao carregar...")` → `showError("Não foi possível carregar. Tente novamente.")`
- Catch de `mostrarAviso("Erro ao buscar entregas")` → `showError("Não foi possível carregar entregas.")`
- Catch de `mostrarAviso("Erro ao enviar respostas")` → `showError("Não foi possível enviar respostas. Tente novamente.")`
- Funções assíncronas de fetch: adicionar try/finally com showLoading/hideLoading

**professor.js:**
- `container.innerHTML = '<p><em>Carregando progresso nas trilhas…</em></p>'` (linha 164) → remover
- Catch blocks → `showError(mensagem amigável específica)`: "Não foi possível carregar turmas.", "Não foi possível carregar desempenho.", "Não foi possível carregar atividades.", "Não foi possível carregar progresso."
- Fetch calls: showLoading/hideLoading

**painel.js:**
- Catch blocks → `showError("Não foi possível carregar o painel.")` e `showError("Não foi possível carregar o histórico.")`
- Fetch calls: showLoading/hideLoading

**turma.js:**
- Função `mostrarErro(msg)` (linha 20): `mostrarAviso(msg, "erro")` → substituir por `showError(msg)`
- `conteudo.innerHTML = "<p>Carregando...</p>"` (linha 125) → remover
- Sucesso `mostrarAviso(msg, "sucesso")` → substituir por `showSuccess(msg)` (turma criada, aluno removido, entrou na turma)
- Catch blocks já usam `mostrarErro()` — após trocar mostrarErro por showError estará correto

**trilhas.js:**
- Função `mostrarErro(msg)` (linha 20): `mostrarAviso(msg, "erro")` → substituir por `showError(msg)`
- `lista.innerHTML = "<p>Carregando...</p>"` (linha 155) → remover
- `conteudo.innerHTML = "<p>Carregando...</p>"` (linha 316) → remover
- Sucesso `mostrarAviso(msg, "sucesso")` → `showSuccess(msg)`

**trilhas-ia.js:**
- Catch blocks `mostrarAviso("Erro ao gerar trilha: " + erro.message)` → `showError("Não foi possível gerar a trilha. Tente novamente.")`
- `mostrarAviso("Erro ao publicar trilha: " + erro.message)` → `showError("Não foi possível publicar a trilha. Tente novamente.")`
- `mostrarAviso("Trilha publicada com sucesso!")` → `showSuccess("Trilha publicada com sucesso!")`
- Fetch calls: showLoading/hideLoading

**Importante — ia-estudo.js NÃO alterar:** esse arquivo usa SSE streaming com indicador de digitação próprio. Não adicionar showLoading/hideLoading. Os `mostrarAviso()` existentes ali podem ser substituídos por `showError()` apenas nos catch blocks de erros de inicialização.
  </action>
  <verify>
    <automated>(Select-String -Path "assets/js/atividades.js","assets/js/professor.js","assets/js/painel.js","assets/js/turma.js","assets/js/trilhas.js","assets/js/trilhas-ia.js","assets/js/aluno.js" -Pattern "showError").Count</automated>
    <automated>(Select-String -Path "assets/js/atividades.js","assets/js/professor.js","assets/js/turma.js","assets/js/trilhas.js" -Pattern "Carregando\.\.\." -CaseSensitive).Count</automated>
    <automated>Select-String -Path "assets/js/turma.js","assets/js/trilhas.js" -Pattern "showSuccess" -Quiet</automated>
  </verify>
  <done>
  - Todos os 7 arquivos JS contêm pelo menos 1 ocorrência de showError (count > 0)
  - Strings "Carregando..." como innerHTML foram removidas de atividades.js, professor.js, turma.js, trilhas.js
  - turma.js e trilhas.js usam showSuccess para confirmações
  - showLoading/hideLoading presente nas funções assíncronas de fetch dos arquivos de produto
  - ia-estudo.js: apenas catch blocks de inicialização atualizados; SSE streaming não alterado
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| API → Browser | Mensagens de erro da API não devem ser expostas literalmente ao usuário — ui.js usa mensagens amigáveis fixas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Information Disclosure | catch blocks expondo erro.message técnico | mitigate | showError(msg amigável) — nunca `showError(erro.message)` com conteúdo técnico de API |
| T-05-11 | Denial of Service | spinner não removido em erro (finally ausente) | mitigate | Regra: hideLoading() SEMPRE em finally{}, nunca só em catch — padrão documentado e aplicado |
| T-05-SC | Tampering | npm (sem novo install neste plano) | accept | Nenhum novo pacote instalado |
</threat_model>

<verification>
Após ambas as tarefas:
1. Abrir pages/aluno.html no browser com servidor local rodando — spinner aparece durante o carregamento inicial de preferências, desaparece após resposta
2. Derrubar o servidor local e tentar carregar pages/professor.html — toast vermelho "Não foi possível carregar..." aparece; página não fica em branco
3. `Select-String -Path "assets/js/ui.js" -Pattern "mostrarAviso"` deve retornar 0 resultados
4. `Select-String -Path "pages/aluno.html","pages/professor.html" -Pattern "ui\.js"` retorna resultados em ambas
5. DevTools Console: nenhum erro de "mostrarAviso is not a function" nas páginas de produto
</verification>

<success_criteria>
- ui.js criado com 4 funções globais; não conflita com mostrarAviso de aluno.js
- 7 páginas HTML importam ui.js antes de aluno.js
- Nenhuma página de produto deixa tela em branco ao falhar fetch — showError visível em todo catch de API
- Spinner aparece e desaparece corretamente (sempre no finally{}) em todas as operações de fetch das páginas de produto
- Textos "Carregando..." hardcoded como innerHTML removidos; spinner global substitui o padrão
</success_criteria>

<output>
Criar `.planning/phases/05-polimento-e-deploy/05-03-SUMMARY.md` ao concluir
</output>
