# Plan 01-03: Migration de BD — Atividades Multi-questão + Pontuação/Streak

**Phase:** 3 — Atividades e Painel do Aluno
**Goal:** Criar as tabelas `questoes` e `respostas_questao` no banco, e adicionar as colunas `pontuacao`, `streak_atual` e `ultimo_acesso` em `usuarios` — sem destruir dados existentes.
**Requirements covered:** ATIV-01, ATIV-02, ATIV-04, ATIV-05, ALUN-03
**Dependencies:** none (primeira execução da fase)
**Estimated complexity:** Medium

---

## Contexto

O schema atual (`database/schema.sql`) já possui as tabelas `atividades` e `entregas`.
Esta migration adiciona as peças que faltam para suportar questões multi-tipo e
pontuação individual de alunos. Deve ser **idempotente**: re-executar não deve
causar erros nem duplicar colunas.

Padrão de referência: `backend/scripts/migration-02.js` e `database/migration-02-trilhas.sql`.

---

## Tasks

### Task 1: Criar `database/migration-03-atividades.sql`

**File:** `database/migration-03-atividades.sql`
**Action:** create

**Description:**
Arquivo SQL puro com os três blocos da migration. Deve iniciar com `USE duopratic;`.
Cada bloco é separado por comentário descritivo.

**Bloco 1 — Tabela `questoes`:**

```sql
USE duopratic;

-- ─── Bloco 1: questões associadas a uma atividade ───────────────────────────
CREATE TABLE IF NOT EXISTS questoes (
    id           INT            AUTO_INCREMENT PRIMARY KEY,
    atividade_id INT            NOT NULL,
    ordem        SMALLINT       NOT NULL,
    tipo         ENUM('multipla_escolha','dissertativa') NOT NULL,
    enunciado    TEXT           NOT NULL,
    opcoes       JSON           NULL,
    -- Para multipla_escolha: array de strings ["a) Texto", "b) Texto", ...]
    -- Para dissertativa: NULL
    gabarito     VARCHAR(10)    NULL,
    -- Para multipla_escolha: "a", "b", "c" ou "d"  (lowercase, sem parênteses)
    -- Para dissertativa: NULL  — corrigida por IA na Phase 4
    criado_em    TIMESTAMP      DEFAULT CURRENT_TIMESTAMP,
    UNIQUE KEY questao_unica (atividade_id, ordem),
    FOREIGN KEY (atividade_id) REFERENCES atividades(id) ON DELETE CASCADE
) ENGINE=InnoDB;
```

**Bloco 2 — Tabela `respostas_questao`:**

```sql
-- ─── Bloco 2: resposta individual de um aluno a uma questão ─────────────────
CREATE TABLE IF NOT EXISTS respostas_questao (
    id            INT        AUTO_INCREMENT PRIMARY KEY,
    aluno_id      INT        NOT NULL,
    questao_id    INT        NOT NULL,
    resposta      TEXT       NOT NULL,
    correta       TINYINT(1) NULL,
    -- NULL  = dissertativa aguardando correção (Phase 4)
    -- 0     = errou (MC)
    -- 1     = acertou (MC)
    respondido_em TIMESTAMP  DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY resposta_unica (aluno_id, questao_id),
    FOREIGN KEY (aluno_id)   REFERENCES usuarios(id)  ON DELETE CASCADE,
    FOREIGN KEY (questao_id) REFERENCES questoes(id)  ON DELETE CASCADE
) ENGINE=InnoDB;
```

**Bloco 3 — ALTER TABLE `usuarios` (idempotente via stored procedure):**

O MySQL 8.0 não suporta `ALTER TABLE ... ADD COLUMN IF NOT EXISTS`.
A solução é encapsular o ALTER em uma stored procedure que captura o erro 1060
(duplicate column name) com `DECLARE CONTINUE HANDLER FOR 1060`.

```sql
-- ─── Bloco 3: colunas de gamificação em usuarios ─────────────────────────────
DROP PROCEDURE IF EXISTS migration03_add_cols;

DELIMITER $$
CREATE PROCEDURE migration03_add_cols()
BEGIN
    DECLARE CONTINUE HANDLER FOR 1060 BEGIN END;
    -- 1060 = ER_DUP_FIELDNAME: coluna já existe — ignorar silenciosamente

    ALTER TABLE usuarios
        ADD COLUMN pontuacao     INT  NOT NULL DEFAULT 0    AFTER perfil,
        ADD COLUMN streak_atual  INT  NOT NULL DEFAULT 0    AFTER pontuacao,
        ADD COLUMN ultimo_acesso DATE NULL                  AFTER streak_atual;
END$$
DELIMITER ;

CALL migration03_add_cols();
DROP PROCEDURE IF EXISTS migration03_add_cols;
```

> **Atenção para o executor:** o arquivo SQL usa `DELIMITER $$` e `DELIMITER ;`.
> Ao rodar via `mysql` CLI diretamente, funciona. Via `mysql2/promise`, o `DELIMITER`
> **não** é suportado — use o script JS (Task 2) que executa cada bloco SQL separadamente.

---

### Task 2: Criar `backend/scripts/migration-03.js`

**File:** `backend/scripts/migration-03.js`
**Action:** create

**Description:**
Script Node.js executado uma única vez: `node backend/scripts/migration-03.js`.
Segue exatamente o padrão de `backend/scripts/migration-02.js`:
- `require("dotenv").config` com path explícito
- `require("../db")` para conexão
- Executa cada instrução SQL separadamente (sem depender de `DELIMITER`)
- Log por etapa
- `process.exit(0)` no sucesso, `process.exit(1)` no erro

**Importante:** como `mysql2` não suporta `DELIMITER`, o ALTER TABLE idempotente
é feito diretamente em JS: executa `ADD COLUMN` dentro de um try/catch que ignora
o código de erro MySQL `ER_DUP_FIELDNAME` (`errno === 1060`).

**Conteúdo completo do arquivo:**

```js
// backend/scripts/migration-03.js
// Executar UMA ÚNICA VEZ: node backend/scripts/migration-03.js
// Cria as tabelas questoes e respostas_questao.
// Adiciona pontuacao, streak_atual, ultimo_acesso em usuarios (idempotente).

require("dotenv").config({ path: require("path").join(__dirname, "../.env") });

const banco = require("../db");

const TABELAS = [
    {
        nome: "questoes",
        sql: `CREATE TABLE IF NOT EXISTS questoes (
            id           INT            AUTO_INCREMENT PRIMARY KEY,
            atividade_id INT            NOT NULL,
            ordem        SMALLINT       NOT NULL,
            tipo         ENUM('multipla_escolha','dissertativa') NOT NULL,
            enunciado    TEXT           NOT NULL,
            opcoes       JSON           NULL,
            gabarito     VARCHAR(10)    NULL,
            criado_em    TIMESTAMP      DEFAULT CURRENT_TIMESTAMP,
            UNIQUE KEY questao_unica (atividade_id, ordem),
            FOREIGN KEY (atividade_id) REFERENCES atividades(id) ON DELETE CASCADE
        ) ENGINE=InnoDB`
    },
    {
        nome: "respostas_questao",
        sql: `CREATE TABLE IF NOT EXISTS respostas_questao (
            id            INT        AUTO_INCREMENT PRIMARY KEY,
            aluno_id      INT        NOT NULL,
            questao_id    INT        NOT NULL,
            resposta      TEXT       NOT NULL,
            correta       TINYINT(1) NULL,
            respondido_em TIMESTAMP  DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
            UNIQUE KEY resposta_unica (aluno_id, questao_id),
            FOREIGN KEY (aluno_id)   REFERENCES usuarios(id)  ON DELETE CASCADE,
            FOREIGN KEY (questao_id) REFERENCES questoes(id)  ON DELETE CASCADE
        ) ENGINE=InnoDB`
    }
];

async function adicionarColunasGamificacao() {
    const colunas = [
        { nome: "pontuacao",     sql: "ALTER TABLE usuarios ADD COLUMN pontuacao     INT  NOT NULL DEFAULT 0    AFTER perfil" },
        { nome: "streak_atual",  sql: "ALTER TABLE usuarios ADD COLUMN streak_atual  INT  NOT NULL DEFAULT 0    AFTER pontuacao" },
        { nome: "ultimo_acesso", sql: "ALTER TABLE usuarios ADD COLUMN ultimo_acesso DATE NULL                  AFTER streak_atual" }
    ];

    for (const col of colunas) {
        try {
            await banco.query(col.sql);
            console.log(`  Coluna 'usuarios.${col.nome}' adicionada.`);
        } catch (erro) {
            if (erro.errno === 1060) {
                console.log(`  Coluna 'usuarios.${col.nome}' já existe — ignorado.`);
            } else {
                throw erro;
            }
        }
    }
}

async function migrar() {
    for (const tabela of TABELAS) {
        await banco.query(tabela.sql);
        console.log(`  Tabela '${tabela.nome}' verificada/criada.`);
    }

    await adicionarColunasGamificacao();

    console.log("Migration-03 concluída com sucesso.");
    process.exit(0);
}

migrar().catch((erro) => {
    console.error("Erro na migration-03:", erro.message);
    process.exit(1);
});
```

---

## Verification

- [ ] `node backend/scripts/migration-03.js` termina com saída `Migration-03 concluída com sucesso.` e exit code 0
- [ ] Re-executar `node backend/scripts/migration-03.js` imediatamente após não gera nenhum erro (idempotente)
- [ ] No MySQL: `DESCRIBE questoes;` exibe colunas `id, atividade_id, ordem, tipo, enunciado, opcoes, gabarito, criado_em`
- [ ] No MySQL: `DESCRIBE respostas_questao;` exibe colunas `id, aluno_id, questao_id, resposta, correta, respondido_em`
- [ ] No MySQL: `DESCRIBE usuarios;` exibe colunas `pontuacao`, `streak_atual`, `ultimo_acesso` após `perfil`
- [ ] `SHOW CREATE TABLE questoes\G` confirma FK para `atividades(id) ON DELETE CASCADE`
- [ ] `SHOW CREATE TABLE respostas_questao\G` confirma FK para `usuarios(id)` e `questoes(id)` ambas `ON DELETE CASCADE`
- [ ] `SHOW CREATE TABLE questoes\G` confirma `UNIQUE KEY questao_unica (atividade_id, ordem)`
- [ ] `SHOW CREATE TABLE respostas_questao\G` confirma `UNIQUE KEY resposta_unica (aluno_id, questao_id)`

## Security Checklist

- [ ] Nenhuma credencial ou secret no arquivo SQL ou no script JS
- [ ] Script lê `.env` via `require("dotenv").config` — não hardcoda host/user/password
- [ ] `gabarito` é coluna no servidor — nunca enviada ao aluno via API (verificado nos PLANs seguintes)
- [ ] `opcoes` usa tipo JSON nativo do MySQL 8.0 — não TEXT/VARCHAR sem validação
