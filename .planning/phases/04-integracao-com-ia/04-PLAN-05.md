# Plano 04-05: Resumos de Desempenho por IA e Painéis

**Phase:** 04 — Integração com IA
**Goal:** Endpoints de análise de desempenho: resumo personalizado para o aluno (pontos fortes e fracos), resumo da turma para o professor e progresso individual nas trilhas. Painéis de `aluno.html` e `professor.html` exibem análises geradas pelo Gemini.
**Depends on:** PLAN-04 (iaController.js e routes/ia.js com gerarTrilha/salvarTrilha já adicionados)
**Requirements covered:** IA-08, IA-09, PROF-01, PROF-02

---

## Contexto

**PROF-01** (desempenho geral da turma por atividade) já tem o endpoint `GET /api/turmas/:id/desempenho` criado na Phase 3 e renderizado em `professor.js`. O que falta é a análise IA em cima desses dados.

**PROF-02** (progresso individual de cada aluno nas trilhas) é novo — precisa de endpoint e renderização.

**IA-08** (resumo do aluno) e **IA-09** (resumo da turma) são análises geradas pelo Gemini com base em dados agregados do banco.

**Schema relevante para queries:**
```sql
-- Acertos/erros por aluno (para IA-08, IA-09, PROF-01):
respostas_questao: aluno_id, questao_id, resposta, correta (1|0|NULL), respondido_em

-- Progresso em trilhas (para PROF-02):
progresso_etapa: aluno_id, etapa_id, concluido TINYINT(1)
trilha_etapas: id, trilha_id, ordem
trilhas: id, turma_id, professor_id, titulo, disciplina

-- Estrutura de turma:
turma_alunos: turma_id, aluno_id
turmas: id, nome, professor_id
```

**Padrão MySQL 8.0 — SUM(IF(...)) em vez de COUNT FILTER:**
```sql
-- Correto (MySQL 8.0):
SUM(IF(rq.correta = 1, 1, 0))  AS acertos
-- Incorreto (PostgreSQL/ANSI, não funciona no MySQL):
COUNT(*) FILTER (WHERE rq.correta = 1)
```

**Padrão de NULLIF para evitar divisão por zero:**
```sql
ROUND(
    SUM(IF(rq.correta=1,100,IF(rq.correta=0,0,NULL)))
    / NULLIF(SUM(IF(rq.correta IS NOT NULL,1,0)), 0)
, 0) AS media_acerto_pct
```

**Resposta JSON esperada do Gemini para `resumoAluno`:**
```json
{
  "resumo": "parágrafo motivador (2-3 frases)",
  "pontos_fortes": ["ponto forte 1", "ponto forte 2"],
  "pontos_fracos": ["área de melhoria 1", "área de melhoria 2"],
  "recomendacao": "sugestão concreta de estudo em 1 frase"
}
```

**Resposta JSON esperada do Gemini para `resumoTurma`:**
```json
{
  "resumo": "análise geral da turma (2-3 frases)",
  "pontos_fortes": ["ponto forte coletivo 1"],
  "pontos_fracos": ["área de melhoria coletiva 1"],
  "recomendacao_professor": "sugestão para o professor (1-2 frases)",
  "alunos_atencao": [{"nome": "João", "situacao": "0% de acerto em múltipla escolha"}]
}
```

---

## Tasks

### Task 1: Adicionar `resumoAluno`, `resumoTurma` e `progressoTrilhasTurma` a `iaController.js` + desbloquear rotas

**File:** `backend/controllers/iaController.js`
**Action:** edit — adicionar três funções ao final do arquivo, antes do `module.exports`

Localizar a linha:
```js
module.exports = { criarSessao, listarSessoes, getMensagens, orientar, gerarTrilha, salvarTrilha };
```

Substituir por (adiciona as três funções acima e atualiza module.exports):

```js
// ── Resumos de desempenho por IA ──────────────────────────────────────────────

/**
 * GET /api/ia/resumo-aluno
 * Agrega dados de desempenho do aluno logado e solicita análise ao Gemini.
 * Retorna: { aluno: { nome, pontuacao, streak_atual }, analise: { resumo, pontos_fortes[], pontos_fracos[], recomendacao } }
 */
async function resumoAluno(req, res) {
    try {
        const alunoId = req.usuario.id;

        // Dados agregados do aluno
        const [dadosRows] = await banco.query(
            `SELECT u.nome, u.pontuacao, u.streak_atual,
                    COUNT(DISTINCT e.atividade_id) AS total_atividades,
                    ROUND(
                        SUM(IF(rq.correta = 1, 100, IF(rq.correta = 0, 0, NULL)))
                        / NULLIF(SUM(IF(rq.correta IS NOT NULL, 1, 0)), 0)
                    , 0) AS media_acerto_pct,
                    SUM(IF(rq.correta = 1, 1, 0)) AS total_acertos,
                    SUM(IF(rq.correta = 0, 1, 0)) AS total_erros
             FROM usuarios u
             LEFT JOIN entregas e ON e.aluno_id = u.id
             LEFT JOIN respostas_questao rq ON rq.aluno_id = u.id
             LEFT JOIN questoes q ON q.id = rq.questao_id AND q.tipo = 'multipla_escolha'
             WHERE u.id = ?
             GROUP BY u.id`,
            [alunoId]
        );

        if (!dadosRows.length) {
            return res.status(404).json({ status: 'erro', message: 'Dados do aluno não encontrados.' });
        }
        const dados = dadosRows[0];

        // Últimas questões erradas para contexto qualitativo (máx 5)
        const [errosRows] = await banco.query(
            `SELECT q.enunciado, a.titulo AS atividade_titulo
             FROM respostas_questao rq
             JOIN questoes q ON q.id = rq.questao_id
             JOIN atividades a ON a.id = q.atividade_id
             WHERE rq.aluno_id = ? AND rq.correta = 0
             ORDER BY rq.respondido_em DESC LIMIT 5`,
            [alunoId]
        );

        const errosTexto = errosRows.length
            ? '\nQuestões erradas recentemente:\n' + errosRows.map((e, i) =>
                `${i + 1}. "${e.enunciado}" (atividade: ${e.atividade_titulo})`
              ).join('\n')
            : '';

        const prompt = `Você é um especialista em pedagogia. Analise o desempenho deste aluno e forneça um resumo personalizado em português do Brasil.

Dados do aluno "${dados.nome}":
- Total de atividades entregues: ${dados.total_atividades || 0}
- Taxa de acerto em múltipla escolha: ${dados.media_acerto_pct !== null ? dados.media_acerto_pct + '%' : 'sem respostas ainda'}
- Total de acertos: ${dados.total_acertos || 0}
- Total de erros: ${dados.total_erros || 0}
- Sequência de estudos (streak): ${dados.streak_atual} dia(s)
- Pontuação acumulada: ${dados.pontuacao} pontos${errosTexto}

Responda SOMENTE com JSON válido, sem markdown, sem texto fora do JSON:
{
  "resumo": "parágrafo motivador de 2 a 3 frases sobre o desempenho geral",
  "pontos_fortes": ["ponto forte 1", "ponto forte 2"],
  "pontos_fracos": ["área de melhoria 1", "área de melhoria 2"],
  "recomendacao": "sugestão concreta de o que estudar agora, em 1 frase"
}`;

        const response = await ai.models.generateContent({
            model: MODELO,
            contents: [{ role: 'user', parts: [{ text: prompt }] }]
        });

        const jsonLimpo = response.text.trim()
            .replace(/^```json\s*/i, '').replace(/^```\s*/i, '').replace(/```\s*$/i, '').trim();

        let analise;
        try {
            analise = JSON.parse(jsonLimpo);
        } catch {
            return res.status(500).json({ status: 'erro', message: 'IA retornou formato inválido. Tente novamente.' });
        }

        res.json({
            status: 'ok',
            aluno: { nome: dados.nome, pontuacao: dados.pontuacao, streak_atual: dados.streak_atual },
            analise
        });
    } catch (erro) {
        res.status(500).json({ status: 'erro', message: 'Erro ao gerar resumo do aluno.', detalhe: erro.message });
    }
}

/**
 * GET /api/ia/resumo-turma/:turmaId
 * Agrega dados da turma e solicita análise ao Gemini.
 * IDOR: turma deve pertencer ao professor logado.
 */
async function resumoTurma(req, res) {
    try {
        const turmaId = parseInt(req.params.turmaId, 10);
        if (isNaN(turmaId)) {
            return res.status(400).json({ status: 'erro', message: 'turmaId inválido.' });
        }

        // IDOR
        const [turmaRows] = await banco.query(
            'SELECT id, nome FROM turmas WHERE id = ? AND professor_id = ? LIMIT 1',
            [turmaId, req.usuario.id]
        );
        if (!turmaRows.length) {
            return res.status(403).json({ status: 'erro', message: 'Turma não encontrada ou acesso negado.' });
        }
        const turmaNome = turmaRows[0].nome;

        // Dados agregados da turma
        const [resumoRows] = await banco.query(
            `SELECT
                COUNT(DISTINCT ta.aluno_id)                                         AS total_alunos,
                ROUND(AVG(sub.media_aluno), 0)                                      AS media_turma_pct,
                SUM(IF(sub.media_aluno >= 70, 1, 0))                                AS acima_meta,
                SUM(IF(sub.media_aluno IS NULL OR sub.media_aluno < 50, 1, 0))      AS precisam_atencao
             FROM turma_alunos ta
             LEFT JOIN (
                 SELECT rq.aluno_id,
                        ROUND(SUM(IF(rq.correta=1,100,IF(rq.correta=0,0,NULL)))
                              / NULLIF(SUM(IF(rq.correta IS NOT NULL,1,0)),0),0) AS media_aluno
                 FROM respostas_questao rq
                 JOIN questoes q ON q.id = rq.questao_id AND q.tipo = 'multipla_escolha'
                 JOIN atividades a ON a.id = q.atividade_id AND a.turma_id = ?
                 GROUP BY rq.aluno_id
             ) sub ON sub.aluno_id = ta.aluno_id
             WHERE ta.turma_id = ?`,
            [turmaId, turmaId]
        );
        const resumo = resumoRows[0];

        // Alunos com menor desempenho (< 50% ou sem submissões) — máx 5 para o prompt
        const [alunosAtencaoRows] = await banco.query(
            `SELECT u.nome,
                    ROUND(SUM(IF(rq.correta=1,100,IF(rq.correta=0,0,NULL)))
                          / NULLIF(SUM(IF(rq.correta IS NOT NULL,1,0)),0),0) AS media_pct,
                    u.streak_atual
             FROM turma_alunos ta
             JOIN usuarios u ON u.id = ta.aluno_id
             LEFT JOIN respostas_questao rq ON rq.aluno_id = u.id
             LEFT JOIN questoes q ON q.id = rq.questao_id AND q.tipo = 'multipla_escolha'
             LEFT JOIN atividades a ON a.id = q.atividade_id AND a.turma_id = ta.turma_id
             WHERE ta.turma_id = ?
             GROUP BY u.id
             HAVING media_pct < 50 OR media_pct IS NULL
             ORDER BY media_pct ASC
             LIMIT 5`,
            [turmaId]
        );

        const alunosTexto = alunosAtencaoRows.length
            ? '\nAlunos com menor desempenho: ' + alunosAtencaoRows.map(a =>
                `${a.nome}${a.media_pct !== null ? ' (' + a.media_pct + '%)' : ' (sem submissões)'}`
              ).join(', ')
            : '';

        const prompt = `Você é um especialista em pedagogia. Analise o desempenho desta turma e forneça orientações para o professor em português do Brasil.

Turma: "${turmaNome}"
- Total de alunos: ${resumo.total_alunos || 0}
- Média geral de acerto: ${resumo.media_turma_pct !== null ? resumo.media_turma_pct + '%' : 'sem dados suficientes'}
- Alunos acima da meta (≥70%): ${resumo.acima_meta || 0}
- Alunos que precisam de atenção (<50% ou sem submissões): ${resumo.precisam_atencao || 0}${alunosTexto}

Responda SOMENTE com JSON válido, sem markdown, sem texto fora do JSON:
{
  "resumo": "análise geral da turma em 2 a 3 frases",
  "pontos_fortes": ["ponto forte coletivo 1"],
  "pontos_fracos": ["área de melhoria coletiva 1"],
  "recomendacao_professor": "sugestão concreta para o professor em 1 ou 2 frases",
  "alunos_atencao": [{"nome": "nome do aluno", "situacao": "breve descrição da situação"}]
}`;

        const response = await ai.models.generateContent({
            model: MODELO,
            contents: [{ role: 'user', parts: [{ text: prompt }] }]
        });

        const jsonLimpo = response.text.trim()
            .replace(/^```json\s*/i, '').replace(/^```\s*/i, '').replace(/```\s*$/i, '').trim();

        let analise;
        try {
            analise = JSON.parse(jsonLimpo);
        } catch {
            return res.status(500).json({ status: 'erro', message: 'IA retornou formato inválido. Tente novamente.' });
        }

        res.json({ status: 'ok', turma: { id: turmaId, nome: turmaNome }, analise });
    } catch (erro) {
        res.status(500).json({ status: 'erro', message: 'Erro ao gerar resumo da turma.', detalhe: erro.message });
    }
}

/**
 * GET /api/ia/progresso-trilhas/:turmaId
 * Retorna progresso individual de cada aluno nas trilhas da turma (PROF-02).
 * IDOR: turma deve pertencer ao professor logado.
 */
async function progressoTrilhasTurma(req, res) {
    try {
        const turmaId = parseInt(req.params.turmaId, 10);
        if (isNaN(turmaId)) {
            return res.status(400).json({ status: 'erro', message: 'turmaId inválido.' });
        }

        // IDOR
        const [check] = await banco.query(
            'SELECT id FROM turmas WHERE id = ? AND professor_id = ? LIMIT 1',
            [turmaId, req.usuario.id]
        );
        if (!check.length) {
            return res.status(403).json({ status: 'erro', message: 'Acesso negado.' });
        }

        // Progresso de cada aluno em cada trilha da turma
        // trilhas.turma_id liga diretamente à turma (sem tabela de junção separada)
        const [progresso] = await banco.query(
            `SELECT u.id AS aluno_id, u.nome AS aluno_nome,
                    t.id AS trilha_id, t.titulo AS trilha_titulo, t.disciplina,
                    COUNT(te.id)                             AS total_etapas,
                    SUM(IF(pe.concluido = 1, 1, 0))          AS etapas_concluidas,
                    ROUND(
                        SUM(IF(pe.concluido = 1, 1, 0)) * 100
                        / NULLIF(COUNT(te.id), 0)
                    , 0)                                     AS progresso_pct
             FROM turma_alunos ta
             JOIN usuarios u ON u.id = ta.aluno_id
             JOIN trilhas t ON t.turma_id = ta.turma_id
             JOIN trilha_etapas te ON te.trilha_id = t.id
             LEFT JOIN progresso_etapa pe ON pe.etapa_id = te.id AND pe.aluno_id = u.id
             WHERE ta.turma_id = ?
             GROUP BY u.id, t.id
             ORDER BY u.nome ASC, t.titulo ASC`,
            [turmaId]
        );

        res.json({ status: 'ok', progresso });
    } catch (erro) {
        res.status(500).json({ status: 'erro', message: 'Erro ao buscar progresso nas trilhas.', detalhe: erro.message });
    }
}

module.exports = {
    criarSessao, listarSessoes, getMensagens, orientar,
    gerarTrilha, salvarTrilha,
    resumoAluno, resumoTurma, progressoTrilhasTurma
};
```

**File:** `backend/routes/ia.js`
**Action:** edit — desbloquear as rotas comentadas de resumo

Localizar:
```js
// ── Resumos de desempenho — implementados em PLAN-05 ─────────────────────────
// roteador.get('/resumo-aluno',           autenticar, verificarAluno,     ia.resumoAluno);
// roteador.get('/resumo-turma/:turmaId',  autenticar, verificarProfessor, ia.resumoTurma);
// roteador.get('/progresso-trilhas/:turmaId', autenticar, verificarProfessor, ia.progressoTrilhasTurma);
```

Substituir por:
```js
// ── Resumos de desempenho ─────────────────────────────────────────────────────
roteador.get('/resumo-aluno',              autenticar, verificarAluno,     ia.resumoAluno);
roteador.get('/resumo-turma/:turmaId',     autenticar, verificarProfessor, ia.resumoTurma);
roteador.get('/progresso-trilhas/:turmaId', autenticar, verificarProfessor, ia.progressoTrilhasTurma);
```

**Commit:** `feat(04-05): iaController — resumoAluno, resumoTurma, progressoTrilhasTurma; routes/ia — desbloquear`

---

### Task 2: Exibir resumo IA no painel do aluno (`painel.js`) e do professor (`professor.js`)

**File:** `assets/js/painel.js`
**Action:** edit — adicionar função `carregarResumoIA` e chamá-la no final

Localizar o final de `painel.js`, onde está a última função e o encerramento do IIFE. A estrutura típica é:

```js
    carregarPainel();
    // ... outras chamadas
})();
```

Adicionar antes do `})();` final:

```js
    // ── Resumo de desempenho por IA ───────────────────────────────────────────
    async function carregarResumoIA() {
        const container = document.getElementById('resumo-ia-aluno');
        if (!container) return; // elemento ausente na página — encerrar silenciosamente

        container.innerHTML = '<p><em>Gerando análise de desempenho…</em></p>';

        try {
            const resp  = await api('/api/ia/resumo-aluno');
            const dados = await resp.json();

            if (dados.status !== 'ok') {
                container.innerHTML = '<p><em>Não foi possível gerar a análise agora.</em></p>';
                return;
            }

            const { analise } = dados;

            const fortesHTML = Array.isArray(analise.pontos_fortes) && analise.pontos_fortes.length
                ? '<ul>' + analise.pontos_fortes.map(p => `<li>${escHtmlPainel(p)}</li>`).join('') + '</ul>'
                : '<p><em>Continue entregando atividades para identificar seus pontos fortes.</em></p>';

            const fracosHTML = Array.isArray(analise.pontos_fracos) && analise.pontos_fracos.length
                ? '<ul>' + analise.pontos_fracos.map(p => `<li>${escHtmlPainel(p)}</li>`).join('') + '</ul>'
                : '<p><em>Sem áreas de melhoria identificadas ainda.</em></p>';

            container.innerHTML = `
                <p>${escHtmlPainel(analise.resumo || '')}</p>
                <div class="linha-titulo" style="margin-top:1rem"><h4>Pontos fortes</h4></div>
                ${fortesHTML}
                <div class="linha-titulo" style="margin-top:1rem"><h4>Pontos de melhoria</h4></div>
                ${fracosHTML}
                ${analise.recomendacao
                    ? `<p style="margin-top:1rem"><strong>Próximo passo:</strong> ${escHtmlPainel(analise.recomendacao)}</p>`
                    : ''}
            `;
        } catch {
            container.innerHTML = '<p><em>Não foi possível carregar a análise de IA.</em></p>';
        }
    }

    function escHtmlPainel(str) {
        return String(str || '')
            .replace(/&/g, '&amp;').replace(/</g, '&lt;')
            .replace(/>/g, '&gt;').replace(/"/g, '&quot;');
    }

    carregarResumoIA();
```

**File:** `pages/aluno.html`
**Action:** edit — adicionar card de resumo IA em `aluno.html`

Localizar um `<section>` ou `<article>` no final da área de conteúdo (antes de `</main>`). Adicionar novo `<article>`:

```html
<article class="cartao-painel" id="resumo-ia-aluno-card">
    <div class="linha-titulo">
        <h2>Análise de desempenho IA</h2>
        <span style="font-size:.85em;opacity:.6">Gemini</span>
    </div>
    <div id="resumo-ia-aluno">
        <p><em>Carregando análise...</em></p>
    </div>
</article>
```

---

**File:** `assets/js/professor.js`
**Action:** edit — adicionar funções `carregarResumoIATurma` e `carregarProgressoTrilhas`

Localizar o final de `professor.js`, antes do `iniciar()` e do `})();` final:

```js
    iniciar();
})();
```

Adicionar as duas funções ANTES de `iniciar()`:

```js
    // ── Resumo IA da turma ────────────────────────────────────────────────────
    async function carregarResumoIATurma(turmaId) {
        const container = document.getElementById('resumo-ia-turma');
        if (!container) return;

        container.innerHTML = '<p><em>Gerando análise da turma com IA…</em></p>';

        try {
            const resp  = await api('/api/ia/resumo-turma/' + turmaId);
            const dados = await resp.json();

            if (dados.status !== 'ok') {
                container.innerHTML = '<p><em>Não foi possível gerar a análise agora.</em></p>';
                return;
            }

            const { analise } = dados;

            const atencaoHTML = Array.isArray(analise.alunos_atencao) && analise.alunos_atencao.length
                ? '<ul>' + analise.alunos_atencao.map(a =>
                    `<li><strong>${escHtmlProf(a.nome)}</strong> — ${escHtmlProf(a.situacao)}</li>`
                  ).join('') + '</ul>'
                : '<p><em>Todos os alunos estão com desempenho satisfatório.</em></p>';

            container.innerHTML = `
                <p>${escHtmlProf(analise.resumo || '')}</p>
                ${analise.recomendacao_professor
                    ? `<p style="margin-top:.8rem"><strong>Recomendação:</strong> ${escHtmlProf(analise.recomendacao_professor)}</p>`
                    : ''}
                <div class="linha-titulo" style="margin-top:1rem"><h4>Alunos que precisam de atenção</h4></div>
                ${atencaoHTML}
            `;
        } catch {
            container.innerHTML = '<p><em>Não foi possível carregar a análise de IA.</em></p>';
        }
    }

    // ── Progresso individual nas trilhas (PROF-02) ────────────────────────────
    async function carregarProgressoTrilhas(turmaId) {
        const container = document.getElementById('progresso-trilhas-container');
        if (!container) return;

        container.innerHTML = '<p><em>Carregando progresso nas trilhas…</em></p>';

        try {
            const resp  = await api('/api/ia/progresso-trilhas/' + turmaId);
            const dados = await resp.json();

            if (dados.status !== 'ok') throw new Error(dados.message);

            if (!dados.progresso.length) {
                container.innerHTML = '<p><em>Nenhum aluno com progresso em trilhas nesta turma.</em></p>';
                return;
            }

            // Agrupar por aluno
            const porAluno = {};
            dados.progresso.forEach(linha => {
                if (!porAluno[linha.aluno_id]) {
                    porAluno[linha.aluno_id] = { nome: linha.aluno_nome, trilhas: [] };
                }
                porAluno[linha.aluno_id].trilhas.push({
                    trilha:    linha.trilha_titulo,
                    disciplina: linha.disciplina,
                    pct:       linha.progresso_pct || 0,
                    concluidas: linha.etapas_concluidas,
                    total:     linha.total_etapas
                });
            });

            container.innerHTML = Object.values(porAluno).map(aluno => `
                <div style="margin-bottom:1rem">
                    <strong>${escHtmlProf(aluno.nome)}</strong>
                    ${aluno.trilhas.map(t => `
                        <div style="margin-left:1rem;margin-top:.3rem">
                            <span>${escHtmlProf(t.trilha)}</span>
                            <span style="margin-left:.5rem;opacity:.7">${t.concluidas}/${t.total} etapas</span>
                            <div class="barra" style="margin-top:.2rem">
                                <span style="width:${t.pct}%"></span>
                            </div>
                        </div>
                    `).join('')}
                </div>
            `).join('');
        } catch (erro) {
            container.innerHTML = '<p class="erro-inline">Erro ao carregar progresso nas trilhas.</p>';
            mostrarAviso('Erro ao carregar progresso: ' + erro.message);
        }
    }

    function escHtmlProf(str) {
        return String(str || '')
            .replace(/&/g, '&amp;').replace(/</g, '&lt;')
            .replace(/>/g, '&gt;').replace(/"/g, '&quot;');
    }
```

E atualizar a função `carregarTurmas` para chamar as novas funções ao carregar a primeira turma.

Localizar em `professor.js`:
```js
        if (dados.turmas.length) {
            // Carrega desempenho da primeira turma automaticamente
            await carregarDesempenho(dados.turmas[0].id, dados.turmas[0].nome);
            await carregarAtividades();
        }
```

Substituir por:
```js
        if (dados.turmas.length) {
            const primeiraId = dados.turmas[0].id;
            // Carrega desempenho da primeira turma automaticamente
            await carregarDesempenho(primeiraId, dados.turmas[0].nome);
            await carregarAtividades();
            // Carrega análise IA e progresso de trilhas
            await carregarResumoIATurma(primeiraId);
            await carregarProgressoTrilhas(primeiraId);
        }
```

**File:** `pages/professor.html`
**Action:** edit — adicionar dois novos cards: resumo IA da turma e progresso nas trilhas

Localizar o card de "Acompanhamento" existente:
```html
<article class="cartao-painel">
    <h2>Acompanhamento</h2>
    <div class="lista-acoes">
        <p><strong>Precisam de atenção</strong><span>5 alunos</span></p>
        <p><strong>Acima da meta</strong><span>14 alunos</span></p>
        <p><strong>Sem acesso recente</strong><span>3 alunos</span></p>
    </div>
</article>
```

Substituir por:
```html
<article class="cartao-painel" id="resumo-ia-turma-card">
    <div class="linha-titulo">
        <h2>Análise IA da turma</h2>
        <span style="font-size:.85em;opacity:.6">Gemini</span>
    </div>
    <div id="resumo-ia-turma">
        <p><em>Carregando análise...</em></p>
    </div>
</article>

<article class="cartao-painel" id="progresso-trilhas-card">
    <div class="linha-titulo">
        <h2>Progresso nas trilhas</h2>
    </div>
    <div id="progresso-trilhas-container">
        <p><em>Carregando progresso...</em></p>
    </div>
</article>
```

**Commit:** `feat(04-05): painel.js e professor.js — resumos IA + progresso de trilhas`

---

## Verification

```bash
# 1. Sintaxe
node --check backend/controllers/iaController.js
node --check assets/js/painel.js
node --check assets/js/professor.js

# 2. Servidor inicia sem erros
node backend/server.js

# 3. Testar endpoint de resumo do aluno (requer token aluno com atividades entregues)
# curl -X GET http://localhost:3000/api/ia/resumo-aluno \
#   -H "Authorization: Bearer $TOKEN_ALUNO"
# Esperado: {"status":"ok","aluno":{...},"analise":{"resumo":"...","pontos_fortes":[...],...}}

# 4. Testar endpoint de resumo da turma (requer token professor)
# curl -X GET http://localhost:3000/api/ia/resumo-turma/1 \
#   -H "Authorization: Bearer $TOKEN_PROF"
# Esperado: {"status":"ok","turma":{...},"analise":{"resumo":"...",...}}

# 5. Testar IDOR no resumo de turma (turma de outro professor → 403)

# 6. Testar progresso de trilhas
# curl -X GET http://localhost:3000/api/ia/progresso-trilhas/1 \
#   -H "Authorization: Bearer $TOKEN_PROF"
# Esperado: {"status":"ok","progresso":[{aluno_nome,trilha_titulo,progresso_pct,...}]}

# 7. Abrir aluno.html como aluno → card "Análise de desempenho IA" deve aparecer e carregar
# 8. Abrir professor.html como professor → cards "Análise IA da turma" e
#    "Progresso nas trilhas" devem aparecer e carregar com dados reais
```

- [ ] `node --check` passa em `iaController.js`, `painel.js` e `professor.js`
- [ ] `GET /api/ia/resumo-aluno` retorna `{ status: 'ok', aluno: {...}, analise: { resumo, pontos_fortes, pontos_fracos, recomendacao } }`
- [ ] `GET /api/ia/resumo-turma/:turmaId` retorna `{ status: 'ok', turma: {...}, analise: {...} }`
- [ ] `GET /api/ia/resumo-turma/:turmaId` com ID de outra turma retorna 403
- [ ] `GET /api/ia/progresso-trilhas/:turmaId` retorna array de progresso por aluno por trilha
- [ ] `aluno.html`: card de análise IA carrega com texto gerado pelo Gemini
- [ ] `professor.html`: card de análise IA da turma exibe resumo + alunos que precisam de atenção
- [ ] `professor.html`: card de progresso nas trilhas exibe barras de progresso por aluno
- [ ] `GET /api/ia/resumo-aluno` por professor retorna 403
- [ ] `GET /api/ia/resumo-turma/:turmaId` por aluno retorna 403
