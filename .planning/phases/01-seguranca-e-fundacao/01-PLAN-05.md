# Plano 5: Refatoração em Módulos + Frontend

**Fase:** 1 — Segurança e Fundação  
**Objetivo:** Dividir `server.js` em `routes/`, `controllers/` e `middleware/`; atualizar `acesso.js` para salvar o token JWT; atualizar `aluno.js` para injetar `Authorization` em todos os fetches; e adicionar guards de autenticação nas 8 páginas protegidas.  
**Requisitos cobertos:** AUTH-03, AUTH-04, AUTH-05 (frontend), INFRA-01  
**Dependências:** PLAN-04 deve estar completo (server.js com JWT + IDOR + sem seed data)  
**Estimativa:** 3 tarefas, ~2,5 horas

---

## Contexto

Após os planos 1–4, o `server.js` está seguro mas ainda monolítico (~300 linhas). O INFRA-01 exige sua divisão em módulos antes das fases seguintes adicionarem mais rotas (Fase 2 adiciona turmas e atividades; Fase 3 adiciona progresso real).

No frontend, dois problemas persistem:
1. `acesso.js` ignora o `token` na resposta do login (linha 40: `return resultado.usuario`) — o token nunca é salvo no localStorage. Todos os fetches com `Authorization` falharão com 401.
2. `aluno.js` não envia `Authorization` em nenhuma das 6 funções `fetch` — após o PLAN-04 exigir JWT em todas as rotas, o frontend quebra completamente.

As 8 páginas protegidas (`aluno.html`, `atividades.html`, `configuracoes.html`, `estudo.html`, `ia-estudo.html`, `professor.html`, `trilhas.html`, `turma.html`) não têm guard de autenticação — qualquer URL direta acessa a página sem login.

**Estrutura de destino após este plano:**
```
backend\
├── server.js                   # Entry point — apenas app.use() e app.listen()
├── db.js                       # Sem alterações
├── .env                        # Já atualizado no PLAN-01
├── middleware\
│   ├── auth.js                 # Já existe do PLAN-03
│   └── errorHandler.js         # Novo — handler centralizado de erros
├── routes\
│   ├── auth.js                 # POST /api/cadastro, POST /api/login
│   ├── usuarios.js             # GET/PUT /api/usuarios/:id e /preferencias
│   └── alunos.js               # GET/POST /api/alunos/:id/progresso/funcoes
└── controllers\
    ├── authController.js       # Lógica de cadastro e login
    ├── usuariosController.js   # Lógica de get/update usuário e preferências
    └── alunosController.js     # Lógica de progresso (sem seed data)

assets\js\
├── acesso.js                   # Editado: salva token no localStorage
├── aluno.js                    # Editado: Authorization em todos os fetches
└── auth-guard.js               # Novo: guard reutilizável para páginas protegidas

pages\
├── aluno.html                  # Editado: inclui auth-guard.js
├── atividades.html             # Editado: inclui auth-guard.js
├── configuracoes.html          # Editado: inclui auth-guard.js
├── estudo.html                 # Editado: inclui auth-guard.js
├── ia-estudo.html              # Editado: inclui auth-guard.js
├── professor.html              # Editado: inclui auth-guard.js
├── trilhas.html                # Editado: inclui auth-guard.js
└── turma.html                  # Editado: inclui auth-guard.js
```

---

## Tarefas

### Tarefa 1: Criar módulos backend (routes/ + controllers/ + errorHandler)

**Arquivo(s):**
- `backend\middleware\errorHandler.js` *(novo)*
- `backend\routes\auth.js` *(novo)*
- `backend\routes\usuarios.js` *(novo)*
- `backend\routes\alunos.js` *(novo)*
- `backend\controllers\authController.js` *(novo)*
- `backend\controllers\usuariosController.js` *(novo)*
- `backend\controllers\alunosController.js` *(novo)*
- `backend\server.js` *(refatorar)*

**Ação:** criar (todos os novos) e substituir (server.js)

---

**`backend\middleware\errorHandler.js`**

Handler centralizado de erro do Express (4 parâmetros — obrigatório para Express reconhecer como error handler):

```js
// backend/middleware/errorHandler.js
// eslint-disable-next-line no-unused-vars
module.exports = function errorHandler(erro, req, res, next) {
    if (erro.code === "ER_DUP_ENTRY") {
        return res.status(409).json({ status: "erro", message: "Este e-mail já está cadastrado." });
    }

    res.status(erro.status || 500).json({
        status: "erro",
        message: erro.message || "Erro interno do servidor.",
        ...(process.env.NODE_ENV !== "production" && { detalhe: erro.stack })
    });
};
```

---

**`backend\controllers\authController.js`**

Extrair a lógica dos handlers `/api/cadastro` e `/api/login` do `server.js` (conforme modificados nos PLAN-02 e PLAN-03):

```js
// backend/controllers/authController.js
const banco = require("../db");
const bcrypt = require("bcrypt");
const jwt = require("jsonwebtoken");

const SALT_ROUNDS = 12;

async function cadastrar(req, res) {
    const { nome, email, senha, perfil } = req.body;

    if (!nome || !email || !senha || !perfil) {
        return res.status(400).json({ status: "erro", message: "Preencha todos os campos." });
    }

    if (!["aluno", "professor"].includes(perfil)) {
        return res.status(400).json({ status: "erro", message: "Perfil inválido." });
    }

    try {
        const hash = await bcrypt.hash(senha, SALT_ROUNDS);
        const [resultado] = await banco.query(
            "INSERT INTO usuarios (nome, email, senha, perfil) VALUES (?, ?, ?, ?)",
            [nome, email, hash, perfil]
        );

        res.status(201).json({
            status: "ok",
            usuario: { id: resultado.insertId, nome, email, perfil }
        });
    } catch (erro) {
        if (erro.code === "ER_DUP_ENTRY") {
            return res.status(409).json({ status: "erro", message: "Este e-mail já está cadastrado." });
        }
        res.status(500).json({ status: "erro", message: "Erro ao cadastrar usuário.", detalhe: erro.message });
    }
}

async function login(req, res) {
    const { email, senha, perfil } = req.body;

    if (!email || !senha || !perfil) {
        return res.status(400).json({ status: "erro", message: "Preencha todos os campos." });
    }

    try {
        const [usuarios] = await banco.query(
            "SELECT id, nome, email, perfil, senha FROM usuarios WHERE email = ? AND perfil = ? LIMIT 1",
            [email, perfil]
        );

        if (!usuarios.length) {
            return res.status(401).json({ status: "erro", message: "E-mail, senha ou perfil inválidos." });
        }

        const usuario = usuarios[0];
        const senhaValida = await bcrypt.compare(senha, usuario.senha);

        if (!senhaValida) {
            return res.status(401).json({ status: "erro", message: "E-mail, senha ou perfil inválidos." });
        }

        const token = jwt.sign(
            { id: usuario.id, perfil: usuario.perfil },
            process.env.JWT_SECRET,
            { expiresIn: "7d" }
        );

        const { senha: _descartada, ...usuarioSeguro } = usuario;
        res.json({ status: "ok", token, usuario: usuarioSeguro });
    } catch (erro) {
        res.status(500).json({ status: "erro", message: "Erro ao entrar.", detalhe: erro.message });
    }
}

module.exports = { cadastrar, login };
```

---

**`backend\controllers\usuariosController.js`**

Extrair handlers de GET/PUT de usuário e preferências (conforme modificados no PLAN-04 com IDOR fix):

```js
// backend/controllers/usuariosController.js
const banco = require("../db");
const bcrypt = require("bcrypt");

const SALT_ROUNDS = 12;

const camposPreferencias = {
    tema: "tema",
    status: "status_usuario",
    foto: "foto_perfil",
    notificacoes: "notificacoes_turma",
    lembretes: "lembretes_estudo",
    disciplina: "disciplina_principal",
    ritmo: "ritmo_semanal"
};

function formatarPreferencias(linha) {
    return {
        tema: linha.tema,
        status: linha.status_usuario,
        foto: linha.foto_perfil || "",
        notificacoes: linha.notificacoes_turma === 1,
        lembretes: linha.lembretes_estudo,
        disciplina: linha.disciplina_principal,
        ritmo: linha.ritmo_semanal
    };
}

async function garantirPreferencias(usuarioId) {
    await banco.query(
        "INSERT IGNORE INTO preferencias_usuario (usuario_id) VALUES (?)",
        [usuarioId]
    );
}

async function getUsuario(req, res) {
    // IDOR — verificação já feita no routes/usuarios.js via verificarProprioUsuario
    try {
        const [usuarios] = await banco.query(
            "SELECT id, nome, email, perfil FROM usuarios WHERE id = ? LIMIT 1",
            [req.params.id]
        );

        if (!usuarios.length) {
            return res.status(404).json({ status: "erro", message: "Usuário não encontrado." });
        }

        res.json({ status: "ok", usuario: usuarios[0] });
    } catch (erro) {
        res.status(500).json({ status: "erro", message: "Erro ao buscar usuário.", detalhe: erro.message });
    }
}

async function atualizarUsuario(req, res) {
    // IDOR — verificação já feita no routes/usuarios.js via verificarProprioUsuario
    const { nome, email, senha } = req.body;
    const campos = [];
    const valores = [];

    if (nome) { campos.push("nome = ?"); valores.push(nome); }
    if (email) { campos.push("email = ?"); valores.push(email); }
    if (senha) {
        campos.push("senha = ?");
        const hash = await bcrypt.hash(senha, SALT_ROUNDS);
        valores.push(hash);
    }

    if (!campos.length) {
        return res.status(400).json({ status: "erro", message: "Nenhum dado para atualizar." });
    }

    try {
        valores.push(req.params.id);
        await banco.query(`UPDATE usuarios SET ${campos.join(", ")} WHERE id = ?`, valores);

        const [usuarios] = await banco.query(
            "SELECT id, nome, email, perfil FROM usuarios WHERE id = ? LIMIT 1",
            [req.params.id]
        );

        res.json({ status: "ok", usuario: usuarios[0] });
    } catch (erro) {
        if (erro.code === "ER_DUP_ENTRY") {
            return res.status(409).json({ status: "erro", message: "Este e-mail já está cadastrado." });
        }
        res.status(500).json({ status: "erro", message: "Erro ao atualizar usuário.", detalhe: erro.message });
    }
}

async function getPreferencias(req, res) {
    // IDOR — verificação já feita no routes/usuarios.js via verificarProprioUsuario
    try {
        const [usuarios] = await banco.query(
            "SELECT id FROM usuarios WHERE id = ? LIMIT 1",
            [req.params.id]
        );

        if (!usuarios.length) {
            return res.status(404).json({ status: "erro", message: "Usuário não encontrado." });
        }

        await garantirPreferencias(req.params.id);

        const [preferencias] = await banco.query(
            "SELECT * FROM preferencias_usuario WHERE usuario_id = ? LIMIT 1",
            [req.params.id]
        );

        res.json({ status: "ok", preferencias: formatarPreferencias(preferencias[0]) });
    } catch (erro) {
        res.status(500).json({ status: "erro", message: "Erro ao buscar preferencias.", detalhe: erro.message });
    }
}

async function atualizarPreferencias(req, res) {
    // IDOR — verificação já feita no routes/usuarios.js via verificarProprioUsuario
    const campos = [];
    const valores = [];

    Object.entries(req.body).forEach(([campo, valor]) => {
        const coluna = camposPreferencias[campo];
        if (!coluna) return;
        campos.push(`${coluna} = ?`);
        valores.push(campo === "notificacoes" ? (valor ? 1 : 0) : valor);
    });

    if (!campos.length) {
        return res.status(400).json({ status: "erro", message: "Nenhuma preferencia para atualizar." });
    }

    try {
        const [usuarios] = await banco.query(
            "SELECT id FROM usuarios WHERE id = ? LIMIT 1",
            [req.params.id]
        );

        if (!usuarios.length) {
            return res.status(404).json({ status: "erro", message: "Usuário não encontrado." });
        }

        await garantirPreferencias(req.params.id);
        valores.push(req.params.id);
        await banco.query(`UPDATE preferencias_usuario SET ${campos.join(", ")} WHERE usuario_id = ?`, valores);

        const [preferencias] = await banco.query(
            "SELECT * FROM preferencias_usuario WHERE usuario_id = ? LIMIT 1",
            [req.params.id]
        );

        res.json({ status: "ok", preferencias: formatarPreferencias(preferencias[0]) });
    } catch (erro) {
        res.status(500).json({ status: "erro", message: "Erro ao salvar preferencias.", detalhe: erro.message });
    }
}

module.exports = { getUsuario, atualizarUsuario, getPreferencias, atualizarPreferencias };
```

---

**`backend\controllers\alunosController.js`**

Extrair handlers de progresso (conforme modificados no PLAN-04 sem seed data):

```js
// backend/controllers/alunosController.js
const banco = require("../db");

async function getProgressoFuncoes(req, res) {
    // IDOR — verificação já feita em routes/alunos.js
    try {
        const alunoId = parseInt(req.params.id, 10);

        const [entregas] = await banco.query(
            `SELECT e.status, e.enviado_em
             FROM entregas e
             JOIN atividades a ON a.id = e.atividade_id
             WHERE e.aluno_id = ?
               AND a.titulo = 'Funcoes do 1 grau'
             LIMIT 1`,
            [alunoId]
        );

        const entrega = entregas[0];
        const concluido = ["entregue", "corrigida"].includes(entrega?.status);

        res.json({
            status: "ok",
            entrega: entrega?.status || "pendente",
            concluido,
            progresso: concluido ? 85 : 72  // hardcoded — será real na Fase 3 (ALUN-01)
        });
    } catch (erro) {
        res.status(500).json({ status: "erro", message: "Erro ao buscar progresso.", detalhe: erro.message });
    }
}

async function salvarProgressoFuncoes(req, res) {
    // IDOR — verificação já feita em routes/alunos.js
    try {
        const alunoId = parseInt(req.params.id, 10);

        const [atividades] = await banco.query(
            "SELECT id FROM atividades WHERE titulo = 'Funcoes do 1 grau' LIMIT 1"
        );

        if (!atividades.length) {
            return res.status(404).json({ status: "erro", message: "Atividade não encontrada." });
        }

        const atividadeId = atividades[0].id;

        await banco.query(
            `INSERT INTO entregas (atividade_id, aluno_id, resposta, status, enviado_em)
             VALUES (?, ?, ?, 'entregue', CURRENT_TIMESTAMP)
             ON DUPLICATE KEY UPDATE
                resposta = VALUES(resposta),
                status = 'entregue',
                enviado_em = CURRENT_TIMESTAMP`,
            [atividadeId, alunoId, "Etapa de Funcoes do 1 grau concluida."]
        );

        res.json({
            status: "ok",
            atividade_id: atividadeId,
            entrega: "entregue",
            concluido: true,
            progresso: 85
        });
    } catch (erro) {
        res.status(500).json({ status: "erro", message: "Erro ao salvar progresso.", detalhe: erro.message });
    }
}

module.exports = { getProgressoFuncoes, salvarProgressoFuncoes };
```

---

**`backend\routes\auth.js`**

```js
// backend/routes/auth.js
const { Router } = require("express");
const rateLimit = require("express-rate-limit");
const authController = require("../controllers/authController");

const roteador = Router();

const limitadorAuth = rateLimit({
    windowMs: 15 * 60 * 1000,
    max: 20,
    standardHeaders: true,
    legacyHeaders: false,
    message: { status: "erro", message: "Muitas tentativas. Tente novamente em 15 minutos." }
});

roteador.post("/cadastro", limitadorAuth, authController.cadastrar);
roteador.post("/login", limitadorAuth, authController.login);

module.exports = roteador;
```

---

**`backend\routes\usuarios.js`**

```js
// backend/routes/usuarios.js
const { Router } = require("express");
const { autenticar } = require("../middleware/auth");
const usuariosController = require("../controllers/usuariosController");

const roteador = Router();

// Middleware IDOR — aluno só acessa seus próprios dados; professor acessa qualquer
function verificarProprioUsuario(req, res, next) {
    const idRequisitado = parseInt(req.params.id, 10);
    if (req.usuario.id !== idRequisitado && req.usuario.perfil !== "professor") {
        return res.status(403).json({ status: "erro", message: "Acesso negado." });
    }
    next();
}

roteador.get("/:id", autenticar, verificarProprioUsuario, usuariosController.getUsuario);
roteador.put("/:id", autenticar, verificarProprioUsuario, usuariosController.atualizarUsuario);
roteador.get("/:id/preferencias", autenticar, verificarProprioUsuario, usuariosController.getPreferencias);
roteador.put("/:id/preferencias", autenticar, verificarProprioUsuario, usuariosController.atualizarPreferencias);

module.exports = roteador;
```

---

**`backend\routes\alunos.js`**

```js
// backend/routes/alunos.js
const { Router } = require("express");
const { autenticar } = require("../middleware/auth");
const alunosController = require("../controllers/alunosController");

const roteador = Router();

// Middleware IDOR — apenas o próprio aluno acessa seu progresso
function verificarProprioAluno(req, res, next) {
    const idRequisitado = parseInt(req.params.id, 10);
    if (req.usuario.id !== idRequisitado) {
        return res.status(403).json({ status: "erro", message: "Acesso negado." });
    }
    next();
}

roteador.get("/:id/progresso/funcoes", autenticar, verificarProprioAluno, alunosController.getProgressoFuncoes);
roteador.post("/:id/progresso/funcoes", autenticar, verificarProprioAluno, alunosController.salvarProgressoFuncoes);

module.exports = roteador;
```

---

**`backend\server.js` — refatorado (substituir conteúdo completo)**

```js
// backend/server.js — Entry point
require("dotenv").config();

const express = require("express");
const path = require("path");
const cors = require("cors");
const helmet = require("helmet");

const rotasAuth = require("./routes/auth");
const rotasUsuarios = require("./routes/usuarios");
const rotasAlunos = require("./routes/alunos");
const errorHandler = require("./middleware/errorHandler");

const app = express();
const porta = process.env.PORT || 3000;
const pastaPublica = path.resolve(__dirname, "..");

app.use(helmet());
app.use(cors({
    origin: process.env.CORS_ORIGIN || "http://localhost:3000",
    methods: ["GET", "POST", "PUT", "DELETE", "OPTIONS"],
    allowedHeaders: ["Content-Type", "Authorization"]
}));
app.use(express.json());
app.use(express.static(pastaPublica));

// Rotas públicas de diagnóstico
app.get("/api/health", (_req, res) => {
    res.json({ status: "ok", message: "Servidor DuoPratic online." });
});

app.get("/api/db/status", async (_req, res) => {
    try {
        const banco = require("./db");
        const [linhas] = await banco.query("SELECT 1 AS conectado");
        res.json({ status: "ok", database: "duopratic", conectado: linhas[0].conectado === 1 });
    } catch (erro) {
        res.status(500).json({
            status: "erro",
            message: "Não foi possível conectar ao MySQL.",
            detalhe: erro.message
        });
    }
});

// Rotas da aplicação
app.use("/api", rotasAuth);
app.use("/api/usuarios", rotasUsuarios);
app.use("/api/alunos", rotasAlunos);

// Frontend
app.get("/", (_req, res) => {
    res.sendFile(path.join(pastaPublica, "index.html"));
});

// Error handler — DEVE ser o último middleware
app.use(errorHandler);

app.listen(porta, () => {
    console.log(`Servidor DuoPratic online na porta ${porta}`);
});
```

**Verificação da Tarefa 1:**
```bash
node backend\server.js
# Deve exibir: "Servidor DuoPratic online na porta 3000" sem erros

curl http://localhost:3000/api/health
# { "status": "ok", "message": "Servidor DuoPratic online." }

curl -X POST http://localhost:3000/api/login -H "Content-Type: application/json" \
  -d "{\"email\":\"teste@test.com\",\"senha\":\"abc123\",\"perfil\":\"aluno\"}"
# { "status": "ok", "token": "eyJ...", "usuario": {...} }
```

---

### Tarefa 2: Atualizar acesso.js e aluno.js para usar JWT

**Arquivo(s):**
- `assets\js\acesso.js` *(editar)*
- `assets\js\aluno.js` *(editar)*

**Ação:** editar

---

**`assets\js\acesso.js` — 2 alterações**

**Alteração 1:** Linha 40 — `enviarAcesso` deve retornar `{ token, usuario }` em vez de apenas `usuario`:

```js
// ANTES (linha 40):
return resultado.usuario;

// DEPOIS:
return { token: resultado.token, usuario: resultado.usuario };
```

**Alteração 2:** Linhas 55–58 — handler do submit deve salvar o token:

```js
// ANTES:
const usuario = await enviarAcesso(dados, tipo);
localStorage.setItem("duopratic_usuario", JSON.stringify(usuario));
localStorage.setItem("duopratic_perfil", usuario.perfil);
window.location.href = destinoPorPerfil(usuario.perfil);

// DEPOIS:
const { token, usuario } = await enviarAcesso(dados, tipo);
localStorage.setItem("duopratic_usuario", JSON.stringify(usuario));
localStorage.setItem("duopratic_perfil", usuario.perfil);
localStorage.setItem("duopratic_token", token);  // salvar token JWT
window.location.href = destinoPorPerfil(usuario.perfil);
```

> **Por que `localStorage` e não `sessionStorage`?** AUTH-04 exige persistência entre recarregamentos. `sessionStorage` é apagada ao fechar a aba. `localStorage` persiste até o logout explícito ou expiração manual.

---

**`assets\js\aluno.js` — 6 alterações (uma por função fetch)**

Adicionar função auxiliar `tokenAtual` logo após a função `usuarioLogado` (linha 17):

```js
function tokenAtual() {
    return localStorage.getItem("duopratic_token") || null;
}
```

Para cada uma das 6 funções que fazem `fetch` para `/api/`, injetar o header `Authorization` e tratar 401:

**`buscarPreferenciasBanco` (linha ~37):**
```js
// ANTES:
const resposta = await fetch(`/api/usuarios/${usuario.id}/preferencias`);

// DEPOIS:
const resposta = await fetch(`/api/usuarios/${usuario.id}/preferencias`, {
    headers: { "Authorization": `Bearer ${tokenAtual()}` }
});
if (resposta.status === 401) {
    localStorage.removeItem("duopratic_token");
    localStorage.removeItem("duopratic_usuario");
    window.location.href = "../pages/login.html";
    return;
}
```

**`salvarPreferenciaBanco` (linha ~56):**
```js
// ANTES:
const resposta = await fetch(`/api/usuarios/${usuario.id}/preferencias`, {
    method: "PUT",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ [campo]: valor })
});

// DEPOIS:
const resposta = await fetch(`/api/usuarios/${usuario.id}/preferencias`, {
    method: "PUT",
    headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${tokenAtual()}`
    },
    body: JSON.stringify({ [campo]: valor })
});
```

**`buscarUsuarioBanco` (linha ~75):**
```js
// ANTES:
const resposta = await fetch(`/api/usuarios/${usuario.id}`);

// DEPOIS:
const resposta = await fetch(`/api/usuarios/${usuario.id}`, {
    headers: { "Authorization": `Bearer ${tokenAtual()}` }
});
if (resposta.status === 401) {
    localStorage.removeItem("duopratic_token");
    localStorage.removeItem("duopratic_usuario");
    window.location.href = "../pages/login.html";
    return;
}
```

**`salvarUsuarioBanco` (linha ~92):**
```js
// ANTES:
const resposta = await fetch(`/api/usuarios/${usuario.id}`, {
    method: "PUT",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ [campo]: valor })
});

// DEPOIS:
const resposta = await fetch(`/api/usuarios/${usuario.id}`, {
    method: "PUT",
    headers: {
        "Content-Type": "application/json",
        "Authorization": `Bearer ${tokenAtual()}`
    },
    body: JSON.stringify({ [campo]: valor })
});
```

**`buscarProgressoFuncoesBanco` (linha ~264):**
```js
// ANTES:
const resposta = await fetch(`/api/alunos/${usuario.id}/progresso/funcoes`);

// DEPOIS:
const resposta = await fetch(`/api/alunos/${usuario.id}/progresso/funcoes`, {
    headers: { "Authorization": `Bearer ${tokenAtual()}` }
});
if (resposta.status === 401) {
    localStorage.removeItem("duopratic_token");
    localStorage.removeItem("duopratic_usuario");
    window.location.href = "../pages/login.html";
    return;
}
```

**`marcarFuncoesConcluidaBanco` (linha ~285):**
```js
// ANTES:
const resposta = await fetch(`/api/alunos/${usuario.id}/progresso/funcoes`, {
    method: "POST"
});

// DEPOIS:
const resposta = await fetch(`/api/alunos/${usuario.id}/progresso/funcoes`, {
    method: "POST",
    headers: { "Authorization": `Bearer ${tokenAtual()}` }
});
```

**Verificação da Tarefa 2:**

Abrir `http://localhost:3000/pages/login.html` no browser, fazer login com usuário existente e confirmar via DevTools > Application > Local Storage:
- `duopratic_usuario` presente (como antes)
- `duopratic_perfil` presente (como antes)
- `duopratic_token` presente com valor `eyJ...` (novo)

Após login, navegar para `aluno.html` e confirmar que DevTools > Network mostra header `Authorization: Bearer eyJ...` nas requisições para `/api/`.

### Tarefa 3: Criar auth-guard.js e incluir nas 8 páginas protegidas

**Arquivo(s):**
- `assets\js\auth-guard.js` *(novo)*
- `pages\aluno.html` *(editar)*
- `pages\atividades.html` *(editar)*
- `pages\configuracoes.html` *(editar)*
- `pages\estudo.html` *(editar)*
- `pages\ia-estudo.html` *(editar)*
- `pages\professor.html` *(editar)*
- `pages\trilhas.html` *(editar)*
- `pages\turma.html` *(editar)*

**Ação:** criar + editar

---

**`assets\js\auth-guard.js`**

```js
// assets/js/auth-guard.js
// Guard de autenticação para páginas protegidas.
// Incluir como PRIMEIRO script de cada página protegida.
// Uso: <body data-perfil-guard="aluno"> ou <body data-perfil-guard="professor">
// Sem data-perfil-guard: verifica apenas autenticação, sem restrição de perfil.

(function guardarPagina() {
    const token = localStorage.getItem("duopratic_token");
    let usuario = null;

    try {
        usuario = JSON.parse(localStorage.getItem("duopratic_usuario"));
    } catch {
        usuario = null;
    }

    if (!token || !usuario) {
        window.location.href = "../pages/login.html";
        return;
    }

    const perfilEsperado = document.body.dataset.perfilGuard;

    if (perfilEsperado && usuario.perfil !== perfilEsperado) {
        window.location.href = usuario.perfil === "professor"
            ? "../pages/professor.html"
            : "../pages/aluno.html";
    }
})();
```

> **IIFE imediata:** o guard executa antes de qualquer outro script da página. Usar IIFE em vez de `DOMContentLoaded` — o check deve acontecer antes do DOM carregar para evitar flash de conteúdo restrito.

> **Sem depender de `duopratic_perfil`:** usa `usuario.perfil` do objeto armazenado — mesma fonte que o resto da aplicação usa via `usuarioLogado()`.

---

**Editar as 8 páginas HTML protegidas:**

Para cada página, localizar a tag `<body>` de abertura e:
1. Adicionar `data-perfil-guard` com o perfil adequado
2. Inserir `<script src="../assets/js/auth-guard.js"></script>` como **primeiro script** (antes de qualquer outro `<script>`)

```html
<!-- pages/aluno.html -->
<body class="area-aluno" data-perfil-guard="aluno">
    <!-- ... conteúdo ... -->
    <script src="../assets/js/auth-guard.js"></script>  <!-- PRIMEIRO script -->
    <script src="../assets/js/tema.js"></script>
    <script src="../assets/js/aluno.js"></script>
</body>
```

```html
<!-- pages/professor.html -->
<body data-perfil-guard="professor">
    <!-- ... -->
    <script src="../assets/js/auth-guard.js"></script>  <!-- PRIMEIRO script -->
    <!-- demais scripts -->
</body>
```

```html
<!-- pages/atividades.html, configuracoes.html, estudo.html, ia-estudo.html, trilhas.html -->
<body data-perfil-guard="aluno">
    <!-- ... -->
    <script src="../assets/js/auth-guard.js"></script>  <!-- PRIMEIRO script -->
    <!-- demais scripts -->
</body>
```

```html
<!-- pages/turma.html — acessada por alunos E professores com funções diferentes -->
<!-- data-perfil-guard="" verifica apenas autenticação, sem restrição de perfil -->
<body data-perfil-guard="">
    <!-- ... -->
    <script src="../assets/js/auth-guard.js"></script>  <!-- PRIMEIRO script -->
    <!-- demais scripts -->
</body>
```

> **`turma.html`:** Página compartilhada entre alunos e professores com funções diferentes. Guard verifica apenas autenticação — a lógica de perfil é tratada internamente na página. (Decisão: 2026-05-22)

> **`login.html` e `cadastro.html`:** NÃO recebem auth-guard — são as páginas de acesso público.

**Verificação da Tarefa 3:**
```bash
# Sem token no localStorage, navegar diretamente para aluno.html
# Abrir DevTools > Console e verificar redirecionamento imediato para login.html

# Ou via curl (verificar que o HTML inclui auth-guard.js):
grep -l "auth-guard.js" pages\*.html
# Deve listar: aluno.html, atividades.html, configuracoes.html, estudo.html,
#              ia-estudo.html, professor.html, trilhas.html, turma.html
```

---

## Critérios de Aceite

- [ ] `node backend\server.js` inicia sem erros — todos os módulos carregam corretamente
- [ ] `POST /api/login` retorna `{ status: "ok", token: "eyJ...", usuario: {...} }`
- [ ] `GET /api/usuarios/:id` sem token retorna 401; com token válido retorna 200
- [ ] `POST /api/login` via browser salva `duopratic_token` no localStorage (visível em DevTools)
- [ ] Requisições em `aluno.html` incluem header `Authorization: Bearer eyJ...` (visível em DevTools > Network)
- [ ] Acessar `pages/aluno.html` sem token no localStorage redireciona para `login.html`
- [ ] Acessar `pages/professor.html` com token de perfil `aluno` redireciona para `aluno.html`
- [ ] `grep -l "auth-guard.js" pages\*.html` retorna as 8 páginas protegidas (não inclui login.html nem cadastro.html)
- [ ] `server.js` tem menos de 60 linhas (apenas entry point com `app.use()` e `app.listen()`)
- [ ] Nenhuma função de handler inline permanece no `server.js`
- [ ] `backend\controllers\`, `backend\routes\` e `backend\middleware\` existem com os arquivos listados
- [ ] Fluxo completo: login → salvar token → carregar aluno.html → buscar preferências/progresso → exibir dados — sem erros no console do browser

## Ameaças e Mitigações

| Ameaça | Mitigação |
|--------|-----------|
| Token expirado causa loop de login | Handler 401 nos fetches de `aluno.js` remove token e redireciona para login — sem loop |
| Flash de conteúdo antes do guard redirecionar | Guard usa IIFE síncrona imediata, sem `DOMContentLoaded` — redireciona antes do render |
| `localStorage` não disponível (modo privado em alguns browsers) | `try/catch` no guard — `usuario = null` na exceção → redireciona para login |
| Token válido mas usuário deletado do banco | O middleware `autenticar` no backend retorna 401 para qualquer token; o frontend trata 401 nos fetches redirecionando para login |
| `auth-guard.js` em login.html/cadastro.html causaria loop infinito | Estas páginas não recebem o script — explicitado no plano |
| Informações sensíveis no payload do JWT decodificável | Payload contém apenas `{ id, perfil }` — nenhum dado pessoal |
