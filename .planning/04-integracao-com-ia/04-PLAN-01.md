# Plano 04-01: Infraestrutura de IA — Gemini SDK, Rate Limiter, Migration

**Phase:** 04 — Integração com IA
**Goal:** Base técnica para todas as funcionalidades de IA: SDK do Gemini instalado e encapsulado em serviço, rate limiter por usuário autenticado, tabelas `sessoes_ia` e `mensagens_ia` criadas no banco.
**Depends on:** Nada (primeiro plano da fase)
**Requirements covered:** IA-04 (rate limit), IA-05 (tabelas para histórico)

---

## Contexto

**Stack:** Node.js + Express + `mysql2/promise`, CommonJS (`require`/`module.exports`).

**Padrões obrigatórios:**
- Migration: mesmo padrão de `backend/scripts/migration-03.js` — carrega `.env` com path explícito, executa cada instrução separadamente, `try/catch errno 1060` para idempotência de colunas.
- `express-rate-limit@8.5.2` já está instalado (Phase 1) — **não reinstalar**.
- `GEMINI_API_KEY` já está em `.env.example` (Phase 1) — deve existir em `.env`.
- Não há arquivo `backend/services/` ainda — criar a pasta.

**@google/genai@2.6.0 — uso correto (SDK v2):**
```js
const { GoogleGenAI } = require('@google/genai');
const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY });

// Non-streaming
const response = await ai.models.generateContent({
    model: 'gemini-2.0-flash',
    contents: [{ role: 'user', parts: [{ text: prompt }] }],
    systemInstruction: { parts: [{ text: systemPrompt }] }
});
const texto = response.text;

// Streaming
const stream = await ai.models.generateContentStream({
    model: 'gemini-2.0-flash',
    contents: messages,   // [{ role: 'user'|'model', parts: [{ text }] }]
    systemInstruction: { parts: [{ text: systemPrompt }] }
});
for await (const chunk of stream) {
    const texto = chunk.text; // pode ser undefined se chunk vazio
}
```

---

## Tasks

### Task 1: Instalar @google/genai e criar `backend/services/ia.js`

**Ação:** Executar `npm install @google/genai@2.6.0` na pasta raiz do projeto (onde está `package.json`).

Em seguida, criar o arquivo de serviço centralizado:

**File:** `backend/services/ia.js`
**Action:** create

```js
// backend/services/ia.js
// Instância única do cliente Gemini — importada por todos os controllers de IA
// Garante que process.env.GEMINI_API_KEY seja lido uma única vez na inicialização

'use strict';

const { GoogleGenAI } = require('@google/genai');

const ai = new GoogleGenAI({ apiKey: process.env.GEMINI_API_KEY });
const MODELO = 'gemini-2.0-flash';

module.exports = { ai, MODELO };
```

**Commit:** `feat(04-01): instalar @google/genai@2.6.0 e criar services/ia.js`

---

### Task 2: Criar `backend/middleware/rateLimit.js`

Rate limiter específico para endpoints de IA. A chave é `req.usuario.id` (não o IP) — **este middleware deve ser aplicado SEMPRE após `autenticar`**, pois depende de `req.usuario` estar populado.

**File:** `backend/middleware/rateLimit.js`
**Action:** create

```js
// backend/middleware/rateLimit.js
// Rate limit para endpoints de IA: 8 req/min por usuário autenticado
//
// ORDEM OBRIGATÓRIA nas rotas:
//   router.post('/orientar', autenticar, verificarAluno, iaRateLimit, handler)
//   ─────────────────────────────────────────────────────────────────
//   autenticar deve vir ANTES — iaRateLimit lê req.usuario.id
//
// express-rate-limit@8.5.2 já instalado na Phase 1 — não reinstalar.

'use strict';

const rateLimit = require('express-rate-limit');

const iaRateLimit = rateLimit({
    windowMs: 60 * 1000,   // janela de 1 minuto
    max: 8,                // máximo 8 requisições por janela por usuário
    standardHeaders: true, // envia RateLimit-* headers (RFC 6585)
    legacyHeaders: false,  // desabilita X-RateLimit-* legados

    // Chave por ID do usuário autenticado (não por IP)
    // req.usuario é populado pelo middleware autenticar
    keyGenerator: (req) => String(req.usuario.id),

    handler: (_req, res) => {
        res.status(429).json({
            status: 'erro',
            message: 'Limite de 8 requisições por minuto atingido. Aguarde um momento antes de tentar novamente.'
        });
    }
});

module.exports = { iaRateLimit };
```

**Commit:** `feat(04-01): middleware/rateLimit.js — 8 req/min por usuário autenticado`

---

### Task 3: Migration — criar tabelas `sessoes_ia` e `mensagens_ia`

**File:** `database/migration-04-ia.sql`
**Action:** create

```sql
-- Migration 04: Sessões e Mensagens de IA
-- Executar UMA VEZ após migrations anteriores aplicadas
-- Seguro para re-execução: CREATE TABLE IF NOT EXISTS

USE duopratic;

-- Sessão de conversa entre aluno e tutor IA
-- Uma sessão agrupa várias mensagens; título é atualizado com o início da 1ª mensagem
CREATE TABLE IF NOT EXISTS sessoes_ia (
    id        INT          AUTO_INCREMENT PRIMARY KEY,
    aluno_id  INT          NOT NULL,
    titulo    VARCHAR(140) NOT NULL DEFAULT 'Nova conversa',
    criado_em TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (aluno_id) REFERENCES usuarios(id)
        ON DELETE CASCADE
) ENGINE=InnoDB;

-- Mensagens trocadas dentro de uma sessão
-- role 'user' = aluno, role 'model' = Gemini
-- conteudo TEXT: suporta até 65 535 bytes — suficiente para respostas longas
CREATE TABLE IF NOT EXISTS mensagens_ia (
    id        INT AUTO_INCREMENT PRIMARY KEY,
    sessao_id INT          NOT NULL,
    role      ENUM('user', 'model') NOT NULL,
    conteudo  TEXT         NOT NULL,
    criado_em TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (sessao_id) REFERENCES sessoes_ia(id)
        ON DELETE CASCADE
) ENGINE=InnoDB;
```

**File:** `backend/scripts/migration-04.js`
**Action:** create

```js
// backend/scripts/migration-04.js
// Cria tabelas sessoes_ia e mensagens_ia
// Uso: node backend/scripts/migration-04.js
// Padrão idêntico a migration-03.js: path explícito + execução separada por instrução

'use strict';

require('dotenv').config({ path: require('path').join(__dirname, '..', '.env') });

const banco = require('../db');

async function executar() {
    console.log('Iniciando migration-04 (tabelas de IA)...');

    // Tabela sessoes_ia
    await banco.query(`
        CREATE TABLE IF NOT EXISTS sessoes_ia (
            id        INT          AUTO_INCREMENT PRIMARY KEY,
            aluno_id  INT          NOT NULL,
            titulo    VARCHAR(140) NOT NULL DEFAULT 'Nova conversa',
            criado_em TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
            FOREIGN KEY (aluno_id) REFERENCES usuarios(id)
                ON DELETE CASCADE
        ) ENGINE=InnoDB
    `);
    console.log("Tabela 'sessoes_ia' verificada/criada.");

    // Tabela mensagens_ia
    await banco.query(`
        CREATE TABLE IF NOT EXISTS mensagens_ia (
            id        INT AUTO_INCREMENT PRIMARY KEY,
            sessao_id INT          NOT NULL,
            role      ENUM('user', 'model') NOT NULL,
            conteudo  TEXT         NOT NULL,
            criado_em TIMESTAMP    DEFAULT CURRENT_TIMESTAMP,
            FOREIGN KEY (sessao_id) REFERENCES sessoes_ia(id)
                ON DELETE CASCADE
        ) ENGINE=InnoDB
    `);
    console.log("Tabela 'mensagens_ia' verificada/criada.");

    console.log('Migration-04 concluída com sucesso.');
    process.exit(0);
}

executar().catch(erro => {
    console.error('Erro na migration-04:', erro.message);
    process.exit(1);
});
```

**Commit:** `feat(04-01): migration-04 — tabelas sessoes_ia e mensagens_ia`

---

## Verification

```bash
# 1. SDK instalado
node -e "const { GoogleGenAI } = require('@google/genai'); console.log('OK')"

# 2. Sintaxe dos novos arquivos
node --check backend/services/ia.js
node --check backend/middleware/rateLimit.js
node --check backend/scripts/migration-04.js

# 3. Executar migration (requer MySQL rodando e .env configurado)
node backend/scripts/migration-04.js
# Saída esperada:
#   Tabela 'sessoes_ia' verificada/criada.
#   Tabela 'mensagens_ia' verificada/criada.
#   Migration-04 concluída com sucesso.

# 4. Idempotência: executar novamente — deve terminar sem erros
node backend/scripts/migration-04.js

# 5. Verificar tabelas no banco
# mysql -u root -p duopratic -e "SHOW TABLES LIKE '%_ia'"
# Deve mostrar: mensagens_ia, sessoes_ia
```

- [ ] `node --check` passa em todos os três arquivos sem erros de sintaxe
- [ ] `backend/services/ia.js` exporta `{ ai, MODELO }` (verificar com `node -e "const s=require('./backend/services/ia'); console.log(typeof s.ai, s.MODELO)"`)
- [ ] `backend/middleware/rateLimit.js` exporta `{ iaRateLimit }` (verificar com `node -e "const r=require('./backend/middleware/rateLimit'); console.log(typeof r.iaRateLimit)"`)
- [ ] Migration executa com exit code 0 na 1ª e 2ª execução
- [ ] Tabelas `sessoes_ia` e `mensagens_ia` existem no banco com FKs corretas
