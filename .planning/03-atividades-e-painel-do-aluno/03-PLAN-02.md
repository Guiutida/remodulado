# Plan 02-03: API de Criação de Atividades (Professor)

**Phase:** 3 — Atividades e Painel do Aluno
**Goal:** Endpoints para o professor criar atividades, adicionar questões e listar/detalhar — com IDOR correto e gabarito nunca exposto ao aluno.
**Requirements covered:** ATIV-01, ATIV-02, ATIV-03, ATIV-06
**Dependencies:** PLAN-01 (tabelas `questoes` devem existir)
**Estimated complexity:** Medium

---

## Contexto

**Stack:** Node.js + Express + `mysql2/promise`, CommonJS apenas (`require`/`module.exports`).

**Padrões obrigatórios (já em uso no projeto):**
- `const [rows] = await banco.query(sql, [params])` — sempre desestruturar
- Sucesso: `res.json({ status: "ok", <chave>: valor })`
- Erro: `res.status(4xx).json({ status: "erro", message: "...", detalhe: err.message })`
- IDOR: `parseInt(req.params.id, 10)` + verificação via `req.usuario.id`
- Middleware de autenticação importado como `{ autenticar }` de `"../middleware/auth"`
- Guards de perfil definidos inline no arquivo de rota (ver `backend/routes/turmas.js`)
- **Rotas literais antes de rotas com parâmetro `:id`** no Express Router

**Arquivos de referência para padrão:**
- `backend/controllers/turmasController.js` — padrão de controller com IDOR
- `backend/routes/turmas.js` — padrão de rota com guards de perfil

---

## Tasks

### Task 1: Criar `backend/controllers/atividadesController.js`

**File:** `backend/controllers/atividadesController.js`
**Action:** create

**Description:**
Controller com todas as funções de atividades. Neste PLAN: `criarAtividade`,
`adicionarQuestao`, `listarProfessor`, `getDetalhe`.
Os PLANs 03 e 04 adicionarão mais funções a este mesmo arquivo.

**Funções privadas (helpers IDOR — não exportadas):**

```js
// Verifica se a atividade pertence ao professor (via JOIN atividades→turmas)
async function verificarDonoAtividade(atividadeId, professorId) {
    const [rows] = await banco.query(
        `SELECT a.id FROM atividades a
         JOIN turmas t ON t.id = a.turma_id
         WHERE a.id = ? AND t.professor_id = ?
         LIMIT 1`,
        [atividadeId, professorId]
    );
    return rows.length > 0;
}

// Verifica se o aluno está matriculado na turma que tem a atividade
async function verificarAlunoNaAtividade(atividadeId, alunoId) {
    const [rows] = await banco.query(
        `SELECT a.id FROM atividades a
         JOIN turma_alunos ta ON ta.turma_id = a.turma_id
         WHERE a.id = ? AND ta.aluno_id = ?
         LIMIT 1`,
        [atividadeId, alunoId]
    );
    return rows.length > 0;
}
```

**Função `criarAtividade`:**
- `POST /api/atividades`
- Requer perfil professor (guard na rota)
- Body esperado: `{ turma_id, titulo, descricao, prazo }`
- Valida: `turma_id`, `titulo` obrigatórios; retorna 400 se ausentes
- IDOR: verifica que a turma pertence ao professor logado usando `verificarDonoDaTurma` local:
  ```js
  const [turmas] = await banco.query(
      "SELECT id FROM turmas WHERE id = ? AND professor_id = ? LIMIT 1",
      [turmaId, req.usuario.id]
  );
  if (!turmas.length) return res.status(403).json({ status: "erro", message: "Turma não encontrada ou acesso negado." });
  ```
- INSERT em `atividades (turma_id, titulo, descricao, prazo)` com valores sanitizados (`.trim()` em strings)
- Retorna: `{ status: "ok", atividade: { id: resultado.insertId, turma_id, titulo, descricao, prazo } }`

**Função `adicionarQuestao`:**
- `POST /api/atividades/:id/questoes`
- Requer perfil professor (guard na rota)
- Parâmetro: `atividadeId = parseInt(req.params.id, 10)`
- IDOR: `verificarDonoAtividade(atividadeId, req.usuario.id)` → 403 se falhar
- Body: `{ tipo, enunciado, opcoes, gabarito }`
- Validações:
  - `tipo` obrigatório e deve ser `"multipla_escolha"` ou `"dissertativa"`
  - `enunciado` obrigatório (não vazio após `.trim()`)
  - Se `tipo === "multipla_escolha"`: `opcoes` deve ser array de 2 a 6 strings, `gabarito` obrigatório
  - Se `tipo === "dissertativa"`: forçar `gabarito = null`, `opcoes = null` (ignorar o que vier no body)
- **`ordem` calculada no servidor** (NUNCA do body):
  ```js
  const [ordemResult] = await banco.query(
      "SELECT COALESCE(MAX(ordem), 0) + 1 AS proxima FROM questoes WHERE atividade_id = ?",
      [atividadeId]
  );
  const ordem = ordemResult[0].proxima;
  ```
- INSERT em `questoes`: `(atividade_id, ordem, tipo, enunciado, opcoes, gabarito)`
  - `opcoes` deve ser serializado com `JSON.stringify(opcoes)` se não for null
- Retorna: `{ status: "ok", questao: { id, atividade_id, ordem, tipo, enunciado } }`
  - **Não retorna `gabarito` na resposta** (nem mesmo para o professor neste endpoint)

**Função `listarProfessor`:**
- `GET /api/atividades`
- Requer perfil professor (guard na rota)
- Lista atividades cujas turmas pertencem ao professor logado
- Query:
  ```sql
  SELECT a.id, a.titulo, a.descricao, a.prazo, a.criado_em,
         t.nome AS turma_nome, t.id AS turma_id,
         COUNT(q.id) AS total_questoes,
         COUNT(e.id) AS total_entregas
  FROM atividades a
  JOIN turmas t ON t.id = a.turma_id
  LEFT JOIN questoes q ON q.atividade_id = a.id
  LEFT JOIN entregas e ON e.atividade_id = a.id
  WHERE t.professor_id = ?
  GROUP BY a.id
  ORDER BY a.criado_em DESC
  ```
- Retorna: `{ status: "ok", atividades: [...] }`

**Função `getDetalhe`:**
- `GET /api/atividades/:id`
- Disponível para professor e aluno (guard só `autenticar` na rota)
- `atividadeId = parseInt(req.params.id, 10)`
- **Ramifica por perfil**:

  **Professor:**
  - IDOR: `verificarDonoAtividade(atividadeId, req.usuario.id)` → 403 se falhar
  - Query questões: inclui `gabarito` no SELECT
    ```sql
    SELECT id, ordem, tipo, enunciado, opcoes, gabarito
    FROM questoes WHERE atividade_id = ? ORDER BY ordem ASC
    ```

  **Aluno:**
  - IDOR: `verificarAlunoNaAtividade(atividadeId, req.usuario.id)` → 403 se falhar
  - Query questões: **EXCLUI `gabarito` do SELECT** — campo não aparece na resposta
    ```sql
    SELECT id, ordem, tipo, enunciado, opcoes
    FROM questoes WHERE atividade_id = ? ORDER BY ordem ASC
    ```

- Em ambos os casos, busca os dados da atividade primeiro:
  ```sql
  SELECT a.id, a.titulo, a.descricao, a.prazo, a.criado_em, t.nome AS turma_nome
  FROM atividades a JOIN turmas t ON t.id = a.turma_id
  WHERE a.id = ? LIMIT 1
  ```
- Se atividade não encontrada (após verificação IDOR): retorna 404
- Para cada questão, parseia `opcoes` de JSON string para array JS se não for null:
  ```js
  questoes = questoes.map(q => ({
      ...q,
      opcoes: q.opcoes ? JSON.parse(q.opcoes) : null
  }));
  ```
- Retorna: `{ status: "ok", atividade: { ...atividadeData, questoes } }`

**Exportações:**
```js
module.exports = { criarAtividade, adicionarQuestao, listarProfessor, getDetalhe };
```

---

### Task 2: Criar `backend/routes/atividades.js`

**File:** `backend/routes/atividades.js`
**Action:** create

**Description:**
Router Express para `/api/atividades`. Segue exatamente o padrão de
`backend/routes/turmas.js`: guards de perfil definidos inline, `autenticar`
importado de `"../middleware/auth"`, rotas literais antes de rotas com `:id`.

**Conteúdo completo:**

```js
// backend/routes/atividades.js
const { Router } = require("express");
const { autenticar } = require("../middleware/auth");
const atividadesController = require("../controllers/atividadesController");

const roteador = Router();

function verificarProfessor(req, res, next) {
    if (req.usuario.perfil !== "professor") {
        return res.status(403).json({ status: "erro", message: "Apenas professores podem executar esta ação." });
    }
    next();
}

function verificarAluno(req, res, next) {
    if (req.usuario.perfil !== "aluno") {
        return res.status(403).json({ status: "erro", message: "Apenas alunos podem executar esta ação." });
    }
    next();
}

// ── Rotas sem parâmetro (literais antes de /:id) ─────────────────────────────
roteador.post("/",  autenticar, verificarProfessor, atividadesController.criarAtividade);
roteador.get("/",   autenticar, verificarProfessor, atividadesController.listarProfessor);

// ── Rotas com parâmetro ───────────────────────────────────────────────────────
roteador.post("/:id/questoes",  autenticar, verificarProfessor, atividadesController.adicionarQuestao);
roteador.get("/:id",            autenticar,                     atividadesController.getDetalhe);
// Nota: getDetalhe faz IDOR check internamente para professor e aluno

module.exports = roteador;
```

---

### Task 3: Montar rota em `backend/server.js`

**File:** `backend/server.js`
**Action:** modify

**Description:**
Adicionar import e mount do router de atividades. Inserir após `rotasTrilhas`
para manter ordem de declaração consistente.

**Mudança 1 — adicionar require** (após linha `const rotasTrilhas = require("./routes/trilhas");`):
```js
const rotasAtividades = require("./routes/atividades");
```

**Mudança 2 — montar rota** (após linha `app.use("/api/trilhas", rotasTrilhas);`):
```js
app.use("/api/atividades", rotasAtividades);
```

Não alterar nenhuma outra linha do arquivo.

---

## Verification

- [ ] `node backend/server.js` inicia sem erros de sintaxe ou `require` faltando
- [ ] `POST /api/atividades` sem token → 401
- [ ] `POST /api/atividades` com token de aluno → 403
- [ ] `POST /api/atividades` com token de professor + body `{ turma_id: <id_de_outra_turma>, titulo: "Teste" }` → 403
- [ ] `POST /api/atividades` com token de professor + body `{ turma_id: <id_própria_turma>, titulo: "Funções do 2º grau", prazo: "2025-12-31" }` → 201 com `{ status: "ok", atividade: { id, ... } }`
- [ ] `POST /api/atividades/:id/questoes` com `tipo: "multipla_escolha"`, `opcoes: ["a) x=1","b) x=2","c) x=3","d) x=4"]`, `gabarito: "b"` → 201 com questão sem campo `gabarito` na resposta
- [ ] `POST /api/atividades/:id/questoes` com `tipo: "dissertativa"` → 201; campo `gabarito` da questão no banco é NULL
- [ ] `POST /api/atividades/:id/questoes` duas vezes → ordens 1 e 2 no banco (servidor calcula `MAX(ordem)+1`)
- [ ] `GET /api/atividades` com token de professor → lista atividades com `total_questoes` e `total_entregas`
- [ ] `GET /api/atividades/:id` com token de professor → questões incluem campo `gabarito`
- [ ] `GET /api/atividades/:id` com token de aluno da turma → questões **não** incluem campo `gabarito`
- [ ] `GET /api/atividades/:id` com token de aluno de outra turma → 403

## Security Checklist

- [ ] `gabarito` nunca aparece em nenhum SELECT que retorna dados para aluno
- [ ] `ordem` nunca lida do `req.body` — sempre calculada via `MAX(ordem)+1`
- [ ] `aluno_id` nunca lido do body — sempre de `req.usuario.id` (JWT)
- [ ] IDOR em todas as rotas: professor só acessa atividades das próprias turmas
- [ ] IDOR em `getDetalhe`: aluno só acessa atividade se estiver na turma correspondente
- [ ] `parseInt(req.params.id, 10)` em toda leitura de parâmetro numérico
- [ ] Sem `eval`, template injection ou execução dinâmica de SQL via concatenação de string
